# API base URL (all endpoints are versioned under /api/v1). Keep the same-origin default in
# development: the Vite dev server proxies /api to the backend.
VITE_API_BASE_URL=/api/v1

# Dev proxy target for /api (read by vite.config.ts)
VITE_API_PROXY_TARGET=http://localhost:5080
