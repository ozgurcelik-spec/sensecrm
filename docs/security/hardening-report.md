# Güvenlik sertleştirme raporu (C-SEC, pilot öncesi)

Kapsam: pilot öncesi güvenlik incelemesinin kalan bulguları (backend). M5 (pilot yayın) daha önce şunları kapattı: Production'da kapalı kendi kendine kayıt
(`Registration:Mode`), dağıtım sertleştirmesi, `ForwardedHeaders`, nginx CSP, dokümantasyonun yalnız Development'ta açık olması, en az yetkili `crm_app` DB rolü,
Conductor'un yayınlanmaması (bkz. [m5-pilot-yayin.md](../plan/m5-pilot-yayin.md)). Mimari ayrıntı: [backend.md §16](../architecture/backend.md). Operasyon: [runbook.md §9.1](../operations/runbook.md).

> **Kimlik eşlemesi hakkında not.** Özgün inceleme metni bu depoda yoktur; kart tanımı yalnız aşağıdaki kimlikleri açıkladı. Açıklanmayan kimlikler (H2, H3, M1, M4, M8, L1, L9, L11)
> tabloda "M5'te kapatılan başlıklarla eşlenir" olarak işaretlidir; hangisinin hangi M5 maddesine karşılık geldiğini özgün metinden Lead doğrulamalıdır.

## Kapanış tablosu

| Kimlik | Bulgu | Durum | Dosyalar | Testler |
|---|---|---|---|---|
| **H1** | Conductor görev girdisine güven (kiracı, lead, rol kimlikleri, onaylayıcı, karar) | **Burada düzeltildi** | `Workflows.Infrastructure/WorkflowsRuntime.cs` (`WorkflowTaskRunner`), `Workflows.Application/Tasks/TaskModel.cs` (`TrustedTask`), `TaskHandlers.cs`, `Triggering/WorkflowTrigger.cs` (`WorkflowInputs.Build`), `Definitions/crm_*.v1.json`, `Sense.Crm.Worker/Workflows/ConductorTaskPollingService.cs` | `TaskInputTrustApiTests` (sahte kiracı, sahte örnek kimliği, bitmiş/bilinmeyen yürütme, tür uyumsuzluğu, sahte rol kimliği, sahte HUMAN çıktısı, karar DB'den) |
| **H2, H3** | (kart tanımında açıklanmadı) | M5'te kapatılan başlıklarla eşlenir — doğrulanmalı | Kayıt kapalı (`Registration:Mode`), `deploy/`, Conductor yayınlanmaz | `PlatformApiTests`, `HostHardeningTests` |
| **H4** | Hesap modeli: yönetici parola seçiyor; mevcut hesap onaysız üye/yönetici oluyor; başka kiracı hesap verisi sızıyor; parola değiştirme yok; zayıf parola; `CreateOrganization` onaysız yönetici | **Burada düzeltildi** (a–f) | `Identity.Application/Members/MemberUseCases.cs`, `Me/AccountUseCases.cs`, `Platform/PlatformUseCases.cs`, `PasswordPolicy.cs`, `Domain/Users/User.cs`, `Domain/Memberships/Membership.cs`, `Infrastructure/Persistence/IdentityReadStore.cs`, `Shared.Web/Middleware/PasswordChangeRequiredMiddleware.cs`, migration `SecurityHardening` | `AccountModelApiTests`, `PlatformApiTests` |
| **M1, M4, M8** | (kart tanımında açıklanmadı) | M5'te kapatılan başlıklarla eşlenir — doğrulanmalı | `ForwardedHeadersSetup`, nginx CSP, `Docs:Enabled` | `HostHardeningTests` |
| **M2** | Kimliği doğrulanmış genel hız sınırı yok; istek gövdesi sınırsız | **Burada düzeltildi** | `Shared.Web/DependencyInjection/WebServiceCollectionExtensions.cs` (`GlobalLimiter`: kullanıcı + kiracı), `Sense.Crm.Api/Program.cs` (Kestrel `MaxRequestBodySize`, `UseRateLimiter` sırası), `RateLimitingOptions`, `RequestLimitsOptions` | `AuthenticatedRequests_AreRateLimited…`, `TenantLimit_IsShared…`, `KestrelRequestBodyLimit_…` |
| **M3** | Giriş: zamanlama farkıyla hesap keşfi, kilit/pasif bilgisi parola doğrulanmadan sızıyor, bilinen e-postayı sonsuz kilitleme | **Burada düzeltildi** (yaklaşım: bkz. backend.md §16.3) | `Identity.Application/Auth/AuthUseCases.cs` (`LoginHandler`), `Infrastructure/Security/TokenAndSecretServices.cs` (`VerifyDummy`, `LoginThrottle`), `Domain/Users/User.cs` | `Login_RevealsLockedOutOrDisabled_OnlyWhen…`, `Login_FromOneIp_CannotLockAKnownAccount…`, `Login_HasAnEmailKeyedRateLimitBucket…`, `UnknownUserVerification_…`, `LoginThrottle_KeysOnIpAndAccount…` |
| **M5** | Refresh token: kayan sınırsız ömür, atomik olmayan dönüşüm (yarış), eşzamanlı isteği hırsızlık sayma | **Burada düzeltildi** | `Identity.Domain/Tokens/RefreshToken.cs`, `Application/Auth/SessionIssuer.cs`, `AuthUseCases.cs` (`RefreshTokenHandler`), `Infrastructure/Persistence/Repositories.cs` (`TryRotateAsync`), migration `SecurityHardening` | `ConcurrentRefresh_…`, `ReusingARotatedToken_WithinTheGraceWindow…`, `…AfterTheGraceWindow…`, `RefreshFamily_HasAnAbsoluteLifetime…` |
| **M6** | İzin unutulan istekler fark edilmiyor | **Burada düzeltildi** | `Shared.Contracts/Security/Permissions.cs` (`AnyAuthenticatedUserAttribute`), tüm `[AnyAuthenticatedUser]` istisnaları (Me, GetOrganization, ListPermissions, GetEntityAudit, auth komutları, davetler, onay liste/tekil/özet, `CreateOrganization`) | `Sense.Crm.Tests.Architecture/RequestAuthorizationTests.cs` |
| **M7** | Delegeli yönetici yetki yükseltebiliyor (kendinde olmayan rol/izin, kendi rolü) | **Burada düzeltildi** | `Members/MemberUseCases.cs` (`DelegationGuard`, `UpdateMemberHandler`), `Roles/RoleUseCases.cs` | `DelegationAndCacheApiTests` |
| **M9** | Tek örnek / izin önbelleği bayatlığı | **Burada düzeltildi + belgelendi** (kod: değişiklikte anında geçersiz kılma, Redis yokken 2 dk; belge: runbook §9.1) | `InfrastructureServiceCollectionExtensions.cs`, `CachingDefaults`, `MemberUseCases.cs`, `AccountUseCases.cs`, `PermissionService.cs` | `RoleUpdate_TakesEffectImmediately…`, `MembershipChanges_…`, `AcceptingAnInvitation_…` |
| **L1, L9, L11** | (kart tanımında açıklanmadı) | Doğrulanmalı (M5 başlıkları ya da kapsam dışı) | — | — |
| **L2** | Aktivite `relatedName` okuma iznine bakmıyor | **Burada düzeltildi** (yer tutucu `***`) | `Activities.Infrastructure/Persistence/ActivityReadStore.cs` | `RelatedNameVisibilityApiTests` |
| **L3** | Fırsat sahibi kendi fırsatının onaylayıcısı olabiliyor | **Burada düzeltildi** (yalnız üye ise `no_approver`) | `TaskHandlers.cs` (`CreateApprovalsTaskHandler`), `Sales.Contracts/RecordLookup.cs` (`GetOwnerUserIdAsync`) | `DealOwner_IsExcludedFromTheApprovers`, `DealOwner_WhoIsTheOnlyApprover_FailsTheExecution…` |
| **L4** | JWT: algoritma sabitlenmemiş, `kid` sabit `crm-dev` | **Burada düzeltildi** | `Sense.Crm.Api/AuthenticationExtensions.cs` (`ValidAlgorithms=[RS256]`, parmak izi `kid`), `appsettings.json` | `Jwt_UsesRs256AndAKeyThumbprintKid_AndRejectsOtherAlgorithms` |
| **L5** | Parola hash yineleme sayısı düşük; giriş parolası sınırsız | **Burada düzeltildi** (210 000 PBKDF2-SHA512, girişte yeniden hash, login max 128) | `TokenAndSecretServices.cs` (`AspNetPasswordHasher`), `LoginValidator` | `PasswordHasher_UsesAtLeast210000Iterations`, `Login_UpgradesLegacyHashes_…`, `Login_RejectsOversizedPasswords_…` |
| **L6** | Sayfalama aritmetiği taşması → 500 | **Burada düzeltildi** | `Shared.Contracts/Paging/PagedQuery.cs` (`SkipFor`), `Shared.Web/Binding/PagedQueryBinder.cs`, `IdentityReadStore.cs` | `AbsurdPagingValues_Return400Validation…`, `ExtremeButRepresentablePagingValues_…` |
| **L7** | Firma web sitesi doğrulanmıyor (`javascript:` vb.) | **Burada düzeltildi** (yalnız yazmada; eski kayıtlar okunur) | `Sales.Application/Common.cs` (`OptionalHttpUrl`), `Accounts/AccountUseCases.cs` | `AccountWebsiteApiTests` |
| **L8** | `BuildLike` LIKE jokerlerini kaçışlamıyor | **Burada düzeltildi** (silinmedi, kaçışlandı) | `Shared.Infrastructure/Querying/GridQueryExtensions.cs` | `GridLikeEscapingTests` |
| **L10** | Kilitsiz bağımlılıklar, susturulmuş zafiyet uyarıları, kayan imaj etiketleri | **Burada düzeltildi** (web imajı hariç) | `packages.lock.json` (36 proje), `Directory.Build.props`, `.github/workflows/ci.yml`, `src/Sense.Crm.*/Dockerfile`, `deploy/docker-compose.prod.yml`, `infra/docker-compose.yml` | Linux'ta `docker build` (kilitli restore) + `dotnet restore --locked-mode` + zafiyet taraması temiz |

## Kabul edilen riskler (gerekçeli)

| Konu | Gerekçe / hafifletme |
|---|---|
| Access token (15 dk) iptal edilemez | İzinler her istekte veritabanı/önbellekten çözülür; pasifleştirilen üye ve rol değişikliği anında etkilenir. JWT'de `perm` claim'i **yoktur** (kart notu "var" diyordu; doğrulandı: yok) — token süresi izin bayatlığını uzatmaz. Parola değişince refresh aileleri kapanır; kalan access token ≤ 15 dk. |
| Giriş azaltma sayaçları bellek içi, kopyalar arası paylaşılmaz | Pilot tek `api` örneğidir (runbook §9.1). Çok kopyada Redis destekli paylaşımlı depo gerekir. Kalıcı koruma: veritabanındaki hesap kilidi + auth IP hız sınırı. |
| Çok IP'li (dağıtık) saldırgan bir hesabı 15 dk'lık aralıklarla kilitleyebilir | Kilit eşiği 10; tek IP+hesap eşiği 5 → tek IP kilitleyemez. Kilitliyken yanlış deneme kilidi uzatmaz; doğru parolalı kullanıcı yalnız beklemek zorundadır. Kademeli kilit artışı bilerek eklenmedi (kaybı büyütür). |
| Kilitli/pasif hesap **doğru parola** verene `locked_out`/`user_disabled` gösterir | Kart gereği: yalnız parolayı bilen kişi görür. |
| Bekleyen davet e-posta varlığını yöneticiye ifşa eder (yeni hesap → `temporaryPassword`, mevcut → `pending`) | Kart sözleşmesi bu ayrımı gerektirir; yalnız `org.users.manage` sahibi görür, hesap verisi (ad, kimlik) sızmaz. |
| E-posta ile parola sıfırlama/MFA yok | E-posta altyapısı yok (M5 açık işi); unutulan parola yönetici müdahalesiyle çözülür. |
| Sahte HUMAN çıktısı workflow'u başarısız yapabilir (karar yoksa) | Karar yazılmaz (veri bütünlüğü korunur); iş sonunda yürütme `decision_not_found` ile başarısız görünür, yönetici yeniden dener. |
| `web/Dockerfile` (node/nginx) etiketleri kayan | **Kapatıldı (C-SEC2 L6):** `node:24.21.0-alpine`, `nginxinc/nginx-unprivileged:1.29.8-alpine` (Docker Hub'da doğrulandı). |

## Ertelenenler / açık işler

- **Merge sonrası `packages.lock.json`:** diğer kartların yeni projeleri (Ticaret/Servis/Pazarlama) kilit dosyasız gelir; Lead merge sonrası `dotnet restore Sense.Crm.slnx` çalıştırıp dosyaları commit etmeli, aksi hâlde CI/Docker `--locked-mode` kırılır. `TestFixture.cs`/`Permissions.cs` çakışmaları yalnız ekleme.
- Yeni modüller için: her yeni `ICommand/IQuery` `[RequiresPermission]` ya da gerekçeli `[AnyAuthenticatedUser]` taşımalı (mimari test zorlar); yeni controller'lar `[Authorize]`.
- Süresi dolmuş refresh token temizliği (KVKK; M5 açık işi), Conductor kimlik doğrulaması, e-posta ile davet/parola sıfırlama, MFA.
- Web tarafı: mevcut hesap davet ekranı (`/me/invitations`), geçici parola akışı (`mustChangePassword`), web sözleşmesi bu raporun kaynağı olan backend ile uyumludur (bkz. backend.md §5).

## Doğrulama (bu dalda çalıştırılanlar)

`dotnet build Sense.Crm.slnx --no-incremental` (0 uyarı / 0 hata), `dotnet format Sense.Crm.slnx --verify-no-changes`, `dotnet test Sense.Crm.slnx --no-build` (tüm modül + mimari + kiracı izolasyon testleri),
kendi veritabanı/portunda canlı duman testi (üye ekleme → geçici parola → parola değiştirme → davet kabul → eşzamanlı refresh → yeniden kullanım). Sonuçlar teslim mesajındadır.

---

# C-SEC2: M7 platform çalışmasının güvenlik incelemesi (H1–H2, M1–M6, L1–L7)

Kapsam: M7 (Platform modülü + konsol + paketleme) sonrası salt-okunur güvenlik incelemesinin bulguları. Bu bölümdeki kimlikler **bu inceleme** içindir (yukarıdaki C-SEC tablosundaki H1… ile karıştırmayın).
Ayrıntı: [backend.md §22.10](../architecture/backend.md), [runbook §12.1–12.4](../operations/runbook.md) (MFA/ikinci onaylayıcı kararı için ADR notu §12.1'dedir).

## Kapanış tablosu

| Kimlik | Bulgu | Durum | Dosyalar | Testler |
|---|---|---|---|---|
| **H1** | Yükseltilen kurulumlarda platform işletim organizasyonu `is_system` olmayabilir (`AccountBackfill` hepsine `false` yazıyordu; `create-platform-admin` yalnız `Created` işaretliyordu) | **Düzeltildi** — aktif platform yöneticisi üyesi olan kiracı hiçbir kipte askıya alınamaz/engellenemez/silinemez/imha edilemez (domain + işleyici + imha işi, dinamik sorgu); `migrate`/`backfill` ve migration `is_system` işaretler; `create-platform-admin` `Promoted`/`Unchanged` için de işaretler; `deploy/smoke.sh` `isSystem` denetimi | `Identity.Contracts/PlatformAdmins.cs`, `Identity.Infrastructure/PlatformSupport.cs` (`PlatformAdminDirectory`), `Platform.Domain/Accounts/TenantAccount.cs`, `Platform.Application/Console/{LifecycleUseCases,StepUpAndAdminUseCases}.cs`, `Platform.Infrastructure/Jobs/{PlatformJobs,TenantErasureJob}.cs`, migration `PlatformSecurityHardening`, `Sense.Crm.Migrator/{PlatformAdminCommand,PlatformCommands}.cs`, `deploy/smoke.sh` | `SecurityHardeningDomainTests`, `StepUpAndProtectionApiTests` (eski biçim: `is_system` false + aktif yönetici üyesi), `LegacyPlatformTenantBackfillApiTests`, `ErasureHardeningApiTests.Job_CancelsBySystem_…` |
| **H2** | Silme talebinde sunucu tarafı onay yok (yazılan ad yalnız web'de) | **Düzeltildi** — zorunlu `confirmTenantName` (`422 platform.confirmation_mismatch`) + **step-up**: çağıranın parolası sunucuda doğrulanır, hız sınırlı/kilitli (`422 platform.step_up_required|step_up_failed`, `429 platform.step_up_rate_limited`; 401 değil); kapsam: silme talebi, `blocked` askı, imha yeniden deneme, platform yöneticisi geri alma; denetim satırı + `TenantDeletionRequested` olayı aynı işlemde; web diyalogları (parola alanı, sunucu hata eşlemesi). **MFA/ikinci onaylayıcı kabul edilen risk** (ADR notu runbook §12.1) | `Identity.Contracts/PlatformAdmins.cs` (`IStepUpAuthenticator`), `Identity.Infrastructure/PlatformSupport.cs` (`StepUpAuthenticator`), `Platform.Application/Console/{LifecycleUseCases,StepUpAndAdminUseCases}.cs`, `Platform.Api/Controllers/PlatformControllers.cs`, `web/src/components/platform/{deletion-request-dialog,suspend-dialog,step-up-*,deletion-panel}.tsx`, `web/src/lib/platform.ts` | `StepUpAndProtectionApiTests` (eksik/yanlış ad, eksik/yanlış parola, hız sınırı, olay + denetim, `blocked` vs `readOnly`, kiracı yöneticisi 403), `platform-step-up.test.ts`, `organization-detail.test.tsx` |
| **M1** | İmha işi yıkıcı işlemden önce yeniden doğrulamıyor; `Start()` koşulsuz; `xmin` yok; oturum düzeyi istişari kilit | **Düzeltildi** — `FOR UPDATE SKIP LOCKED` + durum/vade/`pending_deletion`/sistem/yönetici-üyesi yeniden doğrulaması; `Start()` `Result` döner (cancelled/completed reddedilir); `deletion_requests.xmin` belirteci (migration); işlem düzeyi `pg_try_advisory_xact_lock`; her yıkıcı adımdan önce koruma yeniden denetimi; korunan kiracı → sistemce iptal | `Platform.Domain/Deletion/DeletionRequest.cs`, `Platform.Infrastructure/Jobs/TenantErasureJob.cs`, `Persistence/PlatformDbContext.cs`, migration `PlatformSecurityHardening`, `Sense.Crm.Worker/Platform/PlatformJobServices.cs` | `ErasureHardeningApiTests` (kilitli satır atlanır, iptal→başlat/başlat→iptal, `xmin` çakışması, korunan kiracı sistemce iptal, uygunsuz hesap kalıcı `failed`), `SecurityHardeningDomainTests` (durum makinesi) |
| **M2** | İmha bütünlüğü elle kayıtlı DbContext'lere bağlı; imha sonrası doğrulama yok | **Düzeltildi** — tombstone öncesi `information_schema` taraması (`tenant_id` kolonlu tüm taban tablolar) + `VerifyErasedAsync` kancası; kalıntı → `erasure.verification_failed`, talep `failed`, tombstone yazılmaz; `TenantErasureSteps` sıralı/benzersiz adım kaydı (veritabanı dışı depolar katılabilir); mimari test: katalogdaki her modülün DbContext'i Worker + Migrator'da, her `ITenantEntity` imha planında | `Retention.cs`, `Jobs/ErasureSupport.cs` (`TenantErasureVerifier`), `Jobs/TenantErasureJob.cs`, `tests/Sense.Crm.Tests.Architecture/ErasureRegistrationArchitectureTests.cs` | `ErasureHardeningApiTests` (kayıtsız tablo, dış depo kancası, `information_schema` ↔ imha yolu kapsam testi, adım kaydı), `ErasureRegistrationArchitectureTests` |
| **M3** | Denetim tabloları kurcalamaya açık (`crm_app` tam DML) | **Düzeltildi** — `audit.audit_log_entries` (UPDATE hiç; DELETE yalnız `erasure` işareti + kiracı `pending_deletion/deleted`) ve `platform.platform_audit_entries` (UPDATE yalnız `erasure` + yalnız `target_tenant_name/details`; DELETE yalnız `retention` + 30 günden eski) tetikleyicileri; TRUNCATE reddedilir; süper kullanıcı kırılma-camı; imha/saklama işleri işaretle çalışır | migration'lar `AuditImmutability`, `PlatformSecurityHardening`; `Jobs/{ErasureSupport,TenantErasureJob,PlatformJobs}.cs` (`AuditMaintenance`) | `AuditImmutabilityTests` (süper olmayan rolle her iki yön), tüm imha testleri (tetikleyici altında çalışır) |
| **M4** (kısmi) | Jetonlar `localStorage`'da, 30 gün refresh | **Kısmen düzeltildi** — platform yöneticisi oturumu için yapılandırılabilir kısa aile ömrü (8 saat) ve boşta kalma (60 dk); `HttpOnly` çerez göçü **sonraki iş** (belgelendi, uygulanmadı) | `Identity.Application/{IdentityOptions,Ports,Auth/SessionIssuer}.cs`, `Identity.Infrastructure/Security/TokenAndSecretServices.cs`, `appsettings.json` | `PlatformAdminSessionApiTests` |
| **M5** | `web` konteyneri Postgres/Conductor/Redis ile aynı ağda | **Düzeltildi** — `web` yalnız `frontend` + iç `edge` (api ile); api `edge`+`backend`; diğerleri iç `backend`; `ForwardedHeaders` `edge` alt ağına güvenir | `deploy/docker-compose.prod.yml`, `deploy/.env.example` | Yalıtılmış compose projesinde canlı kanıt (teslim mesajı) |
| **M6** | Platform yöneticisi yaşam döngüsü eksik (revoke yok, bootstrap `MustChangePassword` yok, parola dosyası 644) | **Düzeltildi** — `User.RevokePlatformAdmin/Deactivate/Reactivate`, `revoke-platform-admin` (Migrator) + `GET /platform/admins` / `POST …/revoke` (step-up, son yönetici korunur, oturumlar kapanır), bootstrap `MustChangePassword`, parola dosyası başarıdan sonra boşaltılır, `generate-secrets.sh` 0600 | `Identity.Domain/Users/User.cs`, `PlatformAdminBootstrapper.cs`, `PlatformAdminManager`, `Sense.Crm.Migrator/PlatformAdminCommand.cs`, `Platform.Api/Controllers`, `deploy/generate-secrets.*`, `docker-compose.prod.yml` (yazılabilir bağlama) | `StepUpAndProtectionApiTests` (liste, revoke, step-up, oturum kapanır, son yönetici), `PlatformAdminSessionApiTests`, Identity `PlatformApiTests` (`MustChangePassword`) |
| **L1** | `MarkDeleted` askı gerekçesini/kipini temizlemiyor; serbest metin gerekçeler imhada kalıyor | **Düzeltildi** — `MarkDeleted` `SuspendedReason/SuspensionMode` temizler; `deletion_requests.reason` → `[redacted]`, `platform_audit_entries.details.reason` silinir; UI "kişisel veri yazmayın" uyarısı | `TenantAccount.cs`, `TenantErasureJob.cs`, `web/…/{suspend,deletion-request}-dialog.tsx` | `SecurityHardeningDomainTests`, `ErasureHardeningApiTests.Erasure_RedactsTheFreeTextReasons_…` |
| **L2** | Yedek şifreleme isteğe bağlı; yetim Conductor yürütmeleri | **Düzeltildi** — `backup.sh/.ps1` anahtarsız çalışmayı reddeder (`--no-encryption`/`-NoEncryption` açık bayrak + uyarı); imha işi `engine_workflow_id`'siz yürütmeleri `correlationId` ile Conductor'dan siler | `deploy/backup.{sh,ps1}`, `restore.md`, `WorkflowsContractServices.cs`, `ConductorClient.cs` | `ErasureHardeningApiTests.Erasure_AlsoRemovesOrphanedConductorExecutions_…` (sahte motor: `FindIdsByCorrelationAsync`; gerçek Conductor'la doğrulanmadı, "açık işler"); betiklerin reddi yolu Docker'sız elle doğrulandı |
| **L3** | Başarısız imhayı yeniden deneme yok | **Düzeltildi** — `POST …/deletion-request/retry` (step-up) + konsolda `lastError`/`attempts` ve "Yeniden dene" | `StepUpAndAdminUseCases.cs`, `PlatformControllers.cs`, `web/…/deletion-panel.tsx` | `StepUpAndProtectionApiTests.RetryDeletion_…`, `organization-detail.test.tsx` |
| **L4** | `organization.created` denetimi ayrı işlemde; reddedilen `/platform/**` denemesi izlenmiyor | **Düzeltildi (mümkün olan en iyi)** — doğrudan yazım + `OrganizationCreated` olayından idempotent tamamlama (iki DbContext arasında dağıtık işlem yok); reddedilen 403'ler için kişisel veri içermeyen sayaç + günlük | `OrganizationProvisioner.cs`, `Provisioning.cs`, `AccountProvisioning.cs`, `PlatformRejectionMonitoringMiddleware.cs` | Mevcut `PlatformAuditApiTests` (satır tek kalır); izleme ara katmanı için ayrı test yok (açık işler) |
| **L5** | Askıdayken düşen olay politikası belgesiz | **Belgelendi** (runbook §12.4; `TenantReactivated` uzlaştırma işareti; kod yok) | `docs/operations/runbook.md` | — |
| **L6** | Kayan imaj etiketleri; `TRUSTED_PROXY_CIDR` varsayılanı güvensiz; `smoke.sh` argv'de sır | **Düzeltildi** — `web/Dockerfile` tam yama etiketleri; `TRUSTED_PROXY_CIDR` varsayılanı `127.0.0.1/32` (sahte `X-Forwarded-For` kabul edilmiyor: canlı kanıt); `AllowedHosts` rehberi; `smoke.sh` gövde/başlığı argv'siz gönderir | `web/Dockerfile`, `deploy/{docker-compose.prod.yml,.env.example,smoke.sh}` | Canlı doğrulama (teslim mesajı) |
| **L7** | Davet kabul/red askıdaki/silinen kiracıya üyelik yazabiliyor | **Düzeltildi** — `tenant.suspended` (403) | `AccountUseCases.cs` | `InvitationLifecycleApiTests` (readOnly, blocked, pending_deletion; askı kalkınca kabul çalışır) |

## Kabul edilen riskler (C-SEC2, gerekçeli)

| Konu | Gerekçe / hafifletme |
|---|---|
| MFA ve ikinci onaylayıcı (four-eyes) yok | E-posta/TOTP altyapısı yok; step-up + 7–90 gün iptal edilebilir bekleme + denetim + olay + kısa yönetici oturumu (runbook §12.1 ADR notu). |
| `localStorage` jetonları (M4 tam çözüm) | HttpOnly çerez göçü sonraki iş; kısa platform yöneticisi oturumu ve step-up etkiyi sınırlar. |
| Denetim tetikleyici işareti uygulama tarafından koyulabilir | `erasure` işareti yalnız silme sürecindeki kiracının satırlarını açar; tam çözüm denetimi ayrı salt-ekleme deposuna akıtmaktır (sonraki iş). |
| Bir müşteri organizasyonuna üye olan platform yöneticisi o organizasyonu "korunan" yapar | Bilinçli (H1 kuralı); silmek için önce yetki geri alınır ya da üyelik pasifleştirilir. |
| `web` konteyneri `frontend` ağı üzerinden dışarıya çıkabilir | `web` sır/veri tutmaz, yalnız `api`'ye ulaşır (iç `edge`); yayınlanan port için gerekli (Docker iç ağdan port yayımlayamaz). `api` dışarıya çıkamaz. |
| Step-up/(IP, hesap) sayaçları bellek içi, kopyalar arası paylaşılmaz | Kalıcı koruma hesap kilididir (veritabanı); pilot tek `api` örneği (runbook §9.1). |

## Sonraki / açık işler (C-SEC2)

- Refresh token'ı `HttpOnly` çereze taşımak (CSRF önlemiyle); MFA/ikinci onaylayıcı; denetimi ayrı salt-ekleme deposuna akıtmak.
- Yetim Conductor yürütme temizliği gerçek Conductor'la uçtan uca doğrulanmadı (`GET workflow/{ad}/correlated/{id}` uç noktası varsayıma dayanır; testler sahte motorla çalışır).
- `PlatformRejectionMonitoringMiddleware` için otomatik test yok (sayaç + günlük; elle doğrulama).
- Mevcut `crm-prod` kurulumunun `.env` dosyasında eski `TRUSTED_PROXY_CIDR=10.213.78.0/24` ve `EDGE_SUBNET` yok: yükseltmede güncelleyin (runbook §8).
- `deploy/docker-compose.conductor-ui.yml` UI bindirmesi hâlâ `conductor`'ı `frontend` ağına koyar (geçici, bilerek).

## Doğrulama (C-SEC2, bu dalda çalıştırılanlar)

- **Derleme/biçim/kilit:** `dotnet build Sense.Crm.slnx --no-incremental` 0 uyarı / 0 hata; `dotnet format Sense.Crm.slnx` sonra `--verify-no-changes` çıkış 0; `dotnet restore Sense.Crm.slnx --locked-mode` hatasız (`packages.lock.json` değişmedi).
- **Testler (`dotnet test Sense.Crm.slnx --no-build`, 12 proje, hepsi yeşil, toplam 1240; önceki taban 1188):** Kernel 31, Shared.Infrastructure 20, TenantIsolation 18, Architecture 49, Activities 34, Marketing 101, Sales 76, Workflows 78, Identity 116, Commerce 145, Service 170, Platform 402. Web (`web/`): `tsc --noEmit` 0, `eslint .` 0, `vitest run` 87 dosya / 816 test, `run build` 0.
- **Canlı duman (atılabilir `postgres:17-alpine` :15651, API :5090, Worker, sonra tamamen kaldırıldı):** `migrate` (tetikleyici migration'ları dahil) → `create-platform-admin` (parola dosyası 0 bayta boşaldı; hesap `must_change_password`, işletim kiracısı `is_system`) → bootstrap girişi 403 `auth.password_change_required`, parola değişince konsol açıldı → eski biçim (`is_system=false`) `backfill` ile yeniden işaretlendi → silme talebi: onay adı eksik/yanlış 422 `platform.confirmation_mismatch`, parola eksik 422 `platform.step_up_required`, yanlış 422 `platform.step_up_failed`, doğru 200 (kiracı `pending_deletion`, `TenantDeletionRequested` olayı 1, `deletion.requested` denetimi 1) → kayıtsız tabloya kiracı satırı eklenip vade geçirilince Worker `platform-tombstone: erasure.verification_failed: sales.zz_rogue_live=2` ile `failed` yaptı, hesap `pending_deletion` kaldı → tablo kaldırılıp `retry` (parolasız 422, parolalı 204) → Worker tamamladı: hesap `deleted`/`[deleted]`, askı gerekçesi/kipi `null`, talep gerekçesi `[redacted]`, `details.reason` 0 satır, kiracı denetimi/`identity.tenants` 0 satır, yalnız `TenantErased` olayı kaldı → süper kullanıcı olarak bile `UPDATE platform.platform_audit_entries` `audit_immutable` ile reddedildi → işletim kiracısını askıya alma 422 `platform.system_tenant_protected`, tek yöneticiyi geri alma 409 `platform.last_platform_admin` (Migrator `revoke-platform-admin` de reddetti).
- **Ağ ayrımı (yalıtılmış compose projesi `crm-sec2net`, `git archive HEAD` anlık görüntüsünden derlenen imajlar; proje ve imajlar sonra kaldırıldı):** `web` yalnız `frontend` + iç `edge`'de; `web`'den `postgres`/`conductor`/`worker` konteyner IP'lerine TCP zaman aşımı (`api`'ye açık); `api`'den `curl 1.1.1.1` ve `https://example.com` başarısız; `POST :8383/api/v1/auth/login` nginx üzerinden 200 ve `smoke.sh` (`isSystem:true` denetimi dahil) geçti; sahte `X-Forwarded-For` yeni varsayılanda (`127.0.0.1/32`) yok sayıldı, eski varsayılanda (`10.213.78.0/24`, karşılaştırma) kabul edildi. Uyarı: `web` konteynerinden `postgres` gibi adların çözümlemesi bu makinenin LAN joker DNS'i yüzünden `192.168.1.1` döndürdü (konteyner IP'si değil); bağlantı yine kurulamadı.
