# Backend (.NET) — mimari özet ve HTTP sözleşmesi

Kararlar için bkz. [kararlar.md](kararlar.md). Bu belge Milestone 1 (platform temeli), Milestone 2 (satış çekirdeği), Milestone 3 (aktiviteler + raporlar), Milestone 4 (workflow'lar: Conductor OSS) ve Milestone 5 (pilot yayın: paketleme/dağıtım) sonunda backend'in gerçek durumunu anlatır.
Altyapı, senseik (HR SaaS) deposundan taşınmıştır: aynı teknoloji yığını (.NET 10, EF Core + Npgsql, xUnit v3) ve aynı proje yapısı.

## 1. Proje yapısı

```
Sense.Crm.slnx
src/
  Sense.Crm.Api/              API host'u (Program.cs, JWT, güvenlik başlıkları, correlation id, health)
  Sense.Crm.Worker/           Modüllerin outbox'ını boşaltan arka plan süreci (OutboxPollingService<T>) + Conductor görev işleyicileri ve yürütme durumu senkronu (M4)
  Sense.Crm.Migrator/         EF migration'larını uygular (migrate | reset)
  Shared/
    Sense.Crm.Shared.Kernel/          Entity/AggregateRoot/TenantAggregateRoot, Result/Error, EmailAddress, Money, DateRange, TenantCalendar (kiracı saat dilimi takvimi)
    Sense.Crm.Shared.Contracts/       CQRS arayüzleri, ITenantContext/ICurrentUser, IModule, izinler, sayfalama, integration event
    Sense.Crm.Shared.Infrastructure/  Dispatcher + davranışlar, ModuleDbContext, interceptor'lar, outbox/inbox, denetim, yerelleştirme (resx)
    Sense.Crm.Shared.Web/             ApiControllerBase, izin yetkilendirmesi, hata yönetimi, sürümleme/OpenAPI/CORS/hız sınırı
  Modules/
    Identity/           Domain · Application · Contracts · Infrastructure · Api   (şema: identity)
    Sales/              Domain · Application · Contracts · Infrastructure · Api   (şema: sales; Milestone 2; satış raporları Milestone 3)
    Activities/         Domain · Application · Contracts · Infrastructure · Api   (şema: activities; Milestone 3)
    Workflows/          Domain · Application · Contracts · Infrastructure · Api   (şema: workflows; Milestone 4; Conductor OSS)
    Platform/           Domain · Application · Contracts · Infrastructure · Api   (şema: platform; Milestone 7: planlar/limitler, kiracı yaşam döngüsü, kullanım ölçümü, KVKK imhası, platform konsolu)
    Files/              Domain · Application · Contracts · Infrastructure · Api   (şema: files; Milestone 8C: kayıtlara dosya ekleri, S3 uyumlu nesne depolama, depolama kotası, yaşam döngüsü işleri)
tests/
  Sense.Crm.Tests.Architecture/      Onion/modül sınırı kuralları (NetArchTest)
  Sense.Crm.Tests.TenantIsolation/   EF modelinde kiracı filtresi/indeks denetimi (veritabanısız)
  Sense.Crm.Tests.Shared/            Testcontainers (postgres:17-alpine) + WebApplicationFactory<Program> + Respawn
  Modules/                     Sense.Crm.Modules.Identity.Tests + Sense.Crm.Modules.Sales.Tests + Sense.Crm.Modules.Activities.Tests + Sense.Crm.Modules.Workflows.Tests (birim + HTTP entegrasyon; sahte workflow motoruyla), Shared.Kernel/Infrastructure testleri
infra/docker-compose.yml       geliştirme: postgres + redis + Conductor OSS (host portları standart + 10000, yalnız 127.0.0.1; Conductor 18090)
deploy/                        üretim (M5): docker-compose.prod.yml, .env.example, generate-secrets.*, backup.*, restore.md, smoke.sh, tls/ (bkz. §15)
docs/operations/runbook.md     kurulum, yedek/geri yükleme, yükseltme, izleme, sorun giderme (Türkçe)
build/smoke-workflows.ps1      Gerçek Conductor'a karşı canlı duman testi (M4)
build/new-module.ps1           Yeni modül iskeleti
```

Kurallar (mimari testlerle zorlanır): Domain yalnız Kernel'e bağlıdır; Application, Infrastructure/Api'ye bağlanamaz;
modüller birbirine yalnız `*.Contracts` üzerinden bağlanır; her modülün tek DbContext'i ve kendi şeması vardır; handler'lar `sealed` ve `{Eylem}Handler` adlıdır.

## 2. Senseik'ten alınanlar, bilerek bırakılanlar

**Alınan:** Kernel, Contracts, Infrastructure (Dispatcher; Logging/Validation/Authorization/UnitOfWork/Caching davranışları; HybridCache — Redis isteğe bağlı L2;
`ModuleDbContext` + `AuditTenantInterceptor` + snake_case; outbox/inbox + `OutboxProcessor` + `InProcessEventBus`; correlation id/enricher'lar; resx yerelleştirme tr+en),
Shared.Web, Api host'u, Migrator, Worker, Identity modülünün auth/tenant/user/role/audit çekirdeği, mimari + kiracı izolasyonu testleri, test altyapısı.

**Uyarlananlar:** Kullanıcı hesabı artık **küresel**dir; organizasyonlara `Membership` (kullanıcı ↔ organizasyon ↔ rol) ile bağlanır (K1).
Denetim kaydı ortak `audit.audit_log_entries` tablosuna, her modülde otomatik yazılır (K14). Roller izinleri tek `text[]` kolonunda taşır (basit RBAC, K7).

**Bırakılanlar ve ne zaman geri gelir:**

| Bırakılan | Geri gelme zamanı |
|---|---|
| `EmployeeId`/`eid`, çalışan aramaları, birim yetkilendirme, `DataScope`/`IScopedRequest` (veri kapsamı) | Milestone 3+: kayıt sahipliği bazlı görünürlük (Zoho rol hiyerarşisi + paylaşım kuralları) |
| Impersonation (`act`), API anahtarları, erişim ayarları, modül aç/kapa filtresi, gizlilik/anonimleştirme | SaaS hazırlığı (Milestone 7) / KVKK işleri |
| MFA, e-posta ile davet, parola sıfırlama | Sonraki aşama (e-posta altyapısıyla birlikte). C-SEC'ten beri: yeni hesaba sunucu üretimi **geçici parola** (yanıtta bir kez) ve mevcut hesaba uygulama içi **davet** (bekleyen üyelik, bkz. §16); e-posta gönderimi yine yok |
| Hangfire/zamanlanmış işler | Gerektiğinde (Worker'a eklenir); workflow için Conductor (K9) Milestone 4'te geldi (bkz. §13); zamanlanmış tetikleyiciler hâlâ yok |
| SignalR/bildirimler, MailKit/e-posta | E-posta bildirimi (2. aşama) |
| MinIO/S3 depolama | **Geldi (M8C, K19):** `Files` modülü, `AWSSDK.S3` (sunucudan bağımsız), MinIO topluluk imajı sabit etiketle (bkz. §23) |
| Dışa aktarma (QuestPDF/ClosedXML/CsvHelper), Scriban | Raporlar/CSV içe-dışa aktarma |
| Anthropic/MCP/pgvector, FeatureManagement, RabbitMQ, mailpit | Gereksinim doğarsa |
| Sentry/Seq/Loki/Jaeger/Prometheus/Grafana/OTel exporter'ları | Üretim gözlemlenebilirliği (Kubernetes/Helm, K13). Şimdilik Serilog konsol + istek logu + Npgsql health check |

## 3. Çalıştırma

```powershell
docker compose -f infra/docker-compose.yml up -d          # postgres (localhost:15432), redis (localhost:16379, isteğe bağlı), Conductor (localhost:18090)
dotnet run --project src/Sense.Crm.Migrator                     # migration'ları uygular (audit + identity + sales + activities + workflows); "-- reset" yalnız Development
dotnet run --project src/Sense.Crm.Api                          # http://localhost:5080  (Scalar: /scalar, OpenAPI: /openapi/v1.json)
dotnet run --project src/Sense.Crm.Worker                       # outbox işleyici + Conductor görev işleyicileri + yürütme durumu senkronu
dotnet test Sense.Crm.slnx                                      # entegrasyon testleri için Docker gerekir
```

- Geliştirme bağlantısı `appsettings.Development.json` içindedir (yalnız yerel compose parolası). Üretimde `ConnectionStrings__Database` ve `Auth__SigningKeyPem` ortam değişkeniyle verilir; depoda gizli bilgi yoktur.
- **Üretim başlangıç korumaları (M5):** `Auth:SigningKeyPem` yoksa API başlamaz (Development/Testing hariç); `ConnectionStrings:Database` (appsettings'teki boş şablon değeri dâhil) eksikse Api/Worker/Migrator başlamaz — bu yüzden `dotnet ef` komutları bağlantı dizesi veya `DOTNET_ENVIRONMENT=Development` ister; geçersiz `Registration:Mode` başlatmaz. `/run/secrets/<Ad>` dosyaları yapılandırma anahtarı olur (`__` → `:`, örn. `Auth__SigningKeyPem`). Ters vekil arkasında `ForwardedHeaders:Enabled=true` + `KnownProxies`/`KnownNetworks` (varsayılan kapalı) istemci IP'sini doğru okutur. Dağıtım: [runbook.md](../operations/runbook.md), [m5-pilot-yayin.md](../plan/m5-pilot-yayin.md).
- JWT (RS256): `Auth:SigningKeyPem` boşsa yalnız Development/Testing'de süreç başına geçici anahtar üretilir; diğer ortamlarda uygulama başlamaz. Issuer `crm`, audience `crm-api`.
- Web geliştirme sunucusu `/api` isteklerini `http://localhost:5080`'e yönlendirir; CORS `http://localhost:5173` için açıktır (`Cors:AllowedOrigins`).
- Migration üretimi (yerel araç `dotnet-ef`, `dotnet-tools.json`):
  `dotnet dotnet-ef migrations add <Ad> --project src/Modules/Identity/Sense.Crm.Modules.Identity.Infrastructure --startup-project src/Sense.Crm.Migrator --context IdentityDbContext -o Persistence/Migrations`
  Ortak denetim şeması için `--project src/Shared/Sense.Crm.Shared.Infrastructure --context AuditDbContext`. Mevcut migration'lar: `InitialIdentity`, `InitialAudit`, `InitialSales` (`--project src/Modules/Sales/Sense.Crm.Modules.Sales.Infrastructure --context SalesDbContext`), `InitialActivities` (`--project src/Modules/Activities/Sense.Crm.Modules.Activities.Infrastructure --context ActivitiesDbContext`), `LeadOwnerAssignedAt` (Sales; M4), `InitialWorkflows` (`--project src/Modules/Workflows/Sense.Crm.Modules.Workflows.Infrastructure --context WorkflowsDbContext`), `SecurityHardening` (Identity; C-SEC: `users.must_change_password`, `memberships.status`, `refresh_tokens.family_expires_at`), `InitialPlatform` (`--project src/Modules/Platform/Sense.Crm.Modules.Platform.Infrastructure --context PlatformDbContext`; M7) ve `TenantProfileCompletedAt` (Identity; M7: `tenants.profile_completed_at`). Migrator komutları (M7): `migrate` (şemalar + plan senkronu + backfill), `sync-plans`, `backfill`, `erase-deleted-tenants`, `create-platform-admin` (sistem hesabını da yazar).

## 4. Yeni modül ekleme

1. `./build/new-module.ps1 -Name Sales` — 5 projeyi (Domain/Application/Contracts/Infrastructure/Api) açar, `Sense.Crm.slnx`'e ekler, derler.
2. `src/Sense.Crm.Api/ModuleCatalog.cs`'e `new SalesModule()` ekleyin.
3. `Sense.Crm.Migrator` ve `Sense.Crm.Worker`'a modülün Infrastructure projesini referans verip `AddModuleDbContext` (Worker'da ayrıca `AddModuleHandlers` + `AddHostedService<OutboxPollingService<SalesDbContext>>`) ekleyin.
4. İzinleri `Contracts/SalesPermissions.cs`'te tanımlayın (`crm.<kaynak>.<eylem>`, grup `crm`); modül `IModule.Permissions` ile katalogla paylaşır. Yeni anahtarlar mevcut organizasyonların sistem rollerine API açılışında `SystemRolePermissionSynchronizer` ile yayılır (`SystemRoleDefinitions`: Administrator = tümü, Standard = tüm `crm.*` (M4'ten beri `crm.approvals.decide` hariç: onay yetkisi bilinçli olarak yalnız Administrator'da/özel rollerdedir) + `org.users.read`). Milestone 2'de `crm.accounts/contacts/leads/deals.*` anahtarları Identity.Contracts'taki `CrmPermissions`'tan `SalesPermissions`'a taşındı (anahtar dizgeleri aynı); Milestone 3'te `crm.activities.*` anahtarları `Activities.Contracts.ActivitiesPermissions`'a taşındı (aynı dizgeler); `crm.reports.read` iki modülün (Sales + Activities raporları) ortak izni olduğu için `Identity.Contracts.CrmPermissions.ReportsRead`'de kaldı.
5. Kiracıya ait varlıklar `TenantAggregateRoot`, denetlenecekler `IAuditLogged` olur; global kiracı filtresi, `TenantId` indeksi ve denetim kaydı otomatik gelir. Kiracısız (küresel) bir tablo eklemek bilinçli karardır: `TenantQueryFilterConventionTests.GlobalEntities` listesine eklenir.
6. Handler'ları **Application** assembly'sinden kaydedin (`AddModuleHandlers`; Domain/Contracts assembly'leri de verilir — outbox olay tipleri buradan çözülür). Integration event'i handler içinde `IIntegrationEventOutbox.Enqueue(...)` ile yayınlayın.

## 5. HTTP sözleşmesi (uygulandığı hâliyle)

Taban yol `/api/v1` (URL sürümleme). JSON camelCase, enum'lar camelCase string, `null` alanlar yazılmaz. Tüm uçlar `Authorization: Bearer <accessToken>` ister, `auth/*` uçları hariç.
Access token ~15 dk; refresh token döner (her yenilemede yenisi verilir). Organizasyon token'daki `tid` claim'idir; URL'de taşınmaz.

**Hatalar:** `application/problem+json` (ProblemDetails) + uzantılar `code` (kararlı, noktalı anahtar), `traceId`; doğrulamada ayrıca `errors: { alan: [mesajlar] }` (alan adları camelCase).
`title/detail` `Accept-Language` (tr/en) ile çevrilir; istemci `code`'a göre çalışır.

| code | HTTP | Ne zaman |
|---|---|---|
| `validation` | 400 | Girdi geçersiz (`errors` ile) |
| `auth.unauthenticated` | 401 | Token yok/geçersiz |
| `auth.invalid_credentials` | 401 | E-posta/parola hatalı; **kilitli/pasif hesapta da parola yanlışsa** bu genel kod döner (C-SEC M3); `POST /me/password` yanlış mevcut parola |
| `auth.invalid_refresh_token` | 401 | Refresh token bilinmiyor/süresi dolmuş (token ya da 30 günlük mutlak oturum ömrü)/iptal edilmiş/yeniden kullanılmış |
| `auth.locked_out` | 401 | Yalnız **parola doğruyken** söylenir: ardışık hatalı giriş (tüm IP'lerden 10 deneme → 15 dk kilit) |
| `auth.user_disabled` | 401 | Yalnız parola doğruyken: hesap devre dışı |
| `auth.password_change_required` | 403 | Geçici parolalı hesap; parola değişene kadar `POST /me/password`, `GET /me`, `GET/POST /me/invitations*` ve anonim `auth/*` dışındaki her uç (C-SEC H4) |
| `invitation.not_found` | 404 | Davet yok/başkasına ait/bekleyen değil (varlık sızdırılmaz) |
| `member.cannot_modify_self` | 422 | Kullanıcı kendi rolünü değiştiremez/kendini pasifleştiremez (C-SEC M7) |
| `role.permission_escalation` | 403 | Çağıran kendinde olmayan bir izni içeren rolü oluşturamaz/güncelleyemez/atayamaz (C-SEC M7; Administrator sistem rolü hariç) |
| `auth.no_active_organization` | 403 | Hesabın aktif üyeliği olduğu organizasyon yok |
| `forbidden` | 403 | İzin yok / üyesi olunmayan organizasyona geçiş |
| `not_found` | 404 | Kayıt yok **veya başka organizasyona ait** (varlık sızdırılmaz) |
| `auth.email_taken` | 409 | Kayıtta e-posta zaten var |
| `auth.signup_disabled` | 403 | Herkese açık kayıt kapalı (`Registration:Mode=disabled`, Production varsayılanı; M5) |
| `member.exists` | 409 | Kullanıcı zaten organizasyon üyesi |
| `role.in_use` | 409 | Rol üyelere atanmış |
| `role.name_taken` | 409 | Rol adı kullanımda |
| `role.system_readonly` | 422 | Sistem rolü değiştirilemez/silinemez |
| `member.last_admin` | 422 | Son aktif Administrator pasifleştirilemez/rolü düşürülemez |
| `general.rate_limit_exceeded` | 429 | Auth uçları IP başına dakikada 20 istek (`RateLimiting:Auth`); giriş: aynı IP+hesap 5 hatalı denemeden sonra 15 dk, e-posta başına dakikada 20 deneme (`RateLimiting:LoginEmail`); kimliği doğrulanmış istekler kullanıcı başına dakikada 600 / kiracı başına 3000 (`RateLimiting:User`/`Tenant`; C-SEC M2, M3) |
| `validation` (ek) | 400 | Sayfalama: `page` > 1 000 000 ya da `int`'e sığmayan `page`/`pageSize` (alan `page`/`pageSize`); firma `website` http/https olmalı; parola politikası |
| `tenant.suspended` | 403 | **M7:** salt okunur/engelli kiracıda yazma (deneme bitti, askı, silme bekleyen); `none` erişimde her istek (`/me`, `/subscription`, oturum uçları hariç). `args.reason` ∈ `suspended` `trial_expired` `pending_deletion` `deleted` |
| `plan.module_disabled` | 403 | **M7:** modül planda kapalı (`args.module`); okuma dahil |
| `plan.limit_exceeded` | **402** | **M7:** `maxUsers`/`maxRecords` aşımı (`args.limit`, `args.module?`, `args.max`, `args.used`) |
| `platform.plan_not_found` | 404 | **M7:** plan yok/pasif |
| `platform.invalid_transition` | 409 | **M7:** yaşam döngüsünde tanımsız geçiş (`args.from`, `args.to`) |
| `platform.deletion_not_cancellable` | 409 | **M7:** silme talebi `scheduled` değil ya da bekleme süresi doldu |
| `platform.system_tenant_protected` | 422 | **M7:** sistem (işletim) organizasyonuna askı/silme/plan değişikliği |
| `file.upload_invalid` · `file.too_many_files` · `file.empty` · `file.name_invalid` · `file.extension_change_not_allowed` | 400 | **M8C:** multipart/parça hatası, parça sayısı, boş dosya, temizlenen ad boş, yeniden adlandırmada uzantı değişimi |
| `file.too_large` | **413** | **M8C:** dosya > `MaxFileMb` ya da istek > `MaxRequestMb` (`args.maxBytes`) |
| `file.type_not_allowed` | **415** | **M8C:** uzantı allow-list'te değil / tehlikeli çift uzantı (`args.extension`) |
| `file.content_mismatch` · `file.infected` | 422 | **M8C:** içerik imzası/beyan uyuşmuyor (makro/yürütülebilir dâhil) · tarayıcı zararlı buldu |
| `file.quota_exceeded` | **402** | **M8C:** depolama kotası (`args.maxBytes`, `usedBytes`, `requestedBytes`) |
| `file.quarantined` · `file.content_missing` | 409 · **410** | **M8C:** karantinadaki dosya indirilemez · satır var, nesne yok |
| `file.scan_unavailable` · `file.storage_unavailable` | **503** | **M8C:** tarayıcı hatası + `FailMode=closed` · depo erişilemez/şifreleme doğrulanamadı |
| `file.record_not_found` | 404 | **M8C:** hedef kayıt yok/silinmiş/başka kiracı (liste/yükleme; dosya kimliğiyle `not_found`) |

### Kimlik doğrulama (anonim, hız sınırlı)
- `POST /auth/signup` `{ organizationName, displayName, email, password, locale }` → `AuthResponse`. Yeni organizasyon (slug adından türetilir, `defaultLocale = locale`, `timeZone = Europe/Istanbul`), sistem rolleri (`Administrator`, `Standard`) ve kullanıcı Administrator olarak oluşur. Parola politikası (C-SEC H4-e; kayıt, platform yöneticisi ve parola değiştirmede aynı): 10–128 karakter, e-postanın kullanıcı adı kısmını (≥ 3 karakter) ve gömülü yaygın parola listesini (`CommonPasswords`, ~250 girdi; sondaki rakam/`!` atılarak da denenir) içeremez; ihlal `validation` (alan `password`/`newPassword`). `locale` `tr|en`. **Yalnız `Registration:Mode=open` iken (Development/Testing varsayılanı); kapalıyken (Production varsayılanı) girdi doğrulamasından önce `403 auth.signup_disabled`** (M5).
- `GET /auth/config` → `{ signupEnabled }` (anonim; `Cache-Control: public, max-age=60`, gizli bilgi yok; web "kaydol" bağlantısını buna göre gösterir). Diğer tüm `/auth/*` yanıtları `Cache-Control: no-store` taşır.
- `POST /auth/login` `{ email, password }` → `AuthResponse` (son kullanılan, yoksa ilk aktif organizasyon).
- `POST /auth/refresh` `{ refreshToken }` → `AuthResponse`. Döner (rotating) token; **atomik** dönüşüm (koşullu `UPDATE ... WHERE revoked_at IS NULL`: iki eşzamanlı yenilemeden yalnız biri başarılı olur); kullanılmış token tekrar gelirse tüm aile iptal edilir — **10 sn tolerans**: döndürmeden hemen sonra, döndüren istemciyle aynı istemciden (IP + User-Agent) gelen tekrar reddedilir ama aileyi kapatmaz. Oturum ömrü **mutlaktır** (`RefreshFamilyDays`, 30 gün; dönüşümle uzamaz).
- `POST /auth/logout` `{ refreshToken }` → 204 (bilinmeyen token için de 204).
- `POST /auth/switch-organization` `{ organizationId }` (Bearer) → `AuthResponse`; üye olunmayan organizasyon → `forbidden`.
- `AuthResponse = { accessToken, refreshToken, expiresAt, mustChangePassword }` (`mustChangePassword` her zaman yazılır: `false`/`true`).

### Profil ve katalog
- `POST /me/password` `{ currentPassword, newPassword }` → 200 yeni `AuthResponse` (aktif organizasyon için; `no-store`). Yanlış mevcut parola → 401 `auth.invalid_credentials` (giriş ile aynı IP+hesap sayacı/kilidi), politika ihlali → 400 `validation` (`errors.newPassword`). Başarıda `SecurityStamp` yenilenir, kullanıcının **tüm** refresh token aileleri iptal edilir (bu cihaz dahil; yeni aile açılır), `MustChangePassword` temizlenir. Geçici parolalıyken de çağrılabilir.
- `GET /me/invitations` → `[{ id, organizationId, organizationName, roleName, invitedAt }]` (yalnız çağıranın kendi **bekleyen** davetleri; `id` üyelik satırı kimliği); `POST /me/invitations/{id}/accept|decline` → 204 (yalnız davetli hesap; aksi/bulunamayan 404 `invitation.not_found`). Kabul sonrası organizasyon `me.organizations`'ta görünür ve `switch-organization` ile geçilebilir; reddedilen davet silinir. Geçici parolalıyken de çağrılabilir.
- `GET /me` → `{ user: { id, email, displayName, locale, isPlatformAdmin, mustChangePassword }, organization: { id, name, slug, defaultLocale, timeZone }, role: { id, name }, permissions: string[], organizations: [{ id, name, slug }] }`
- `PATCH /me` `{ displayName?, locale? }` → 204
- `GET /permissions` → `[{ key, group }]` (`group`: `org` | `crm`); 18 anahtar: `org.settings.manage, org.users.read, org.users.manage, org.roles.manage, org.audit.read`, `crm.{accounts,contacts,leads,deals}.{read,write}` (Sales modülü), `crm.activities.{read,write}` (Activities modülü), `crm.reports.read` (Identity.Contracts, ortak) ve M4'te `org.workflows.manage` + `crm.approvals.decide` (Workflows modülü).

### Organizasyon
- `GET /organization` → `{ id, name, slug, defaultLocale, timeZone }` (aktif üye); `PUT /organization` `{ name, defaultLocale, timeZone }` → 204 (`org.settings.manage`; `timeZone` geçerli IANA kimliği olmalı)
- `GET /organization/members` → `[{ userId, email, displayName, roleId, roleName, isActive, joinedAt, status: "active"|"pending", invitedAt? }]` (`org.users.read`). **Bekleyen** davetler yalnız e-posta ile görünür: `displayName` boş, `userId` yerine davetin (üyelik satırının) kimliği, `isActive: false`, `invitedAt` dolu — başka organizasyonlarla paylaşılan hesabın kimliği/adı sızmaz.
- `POST /organization/members` `{ email, displayName, roleId }` → 201 (`org.users.manage`; **parola alanı yok**, yönetici parola seçmez; yanıt `Cache-Control: no-store`). Yeni e-posta: hesap sunucu üretimi tek seferlik parolayla açılır, `MustChangePassword = true`, üyelik aktif → `{ userId, email, roleId, roleName, status: "active", temporaryPassword }` (`displayName` zorunlu). Mevcut e-posta: **bekleyen davet** (otomatik katılım yok) → `{ email, roleId, roleName, status: "pending" }` (`userId`/`temporaryPassword`/ad yok). Zaten üye ya da bekleyen davet varsa `member.exists`. Verilen rolün izinleri çağıranın izinlerinin alt kümesi olmalı (`role.permission_escalation`; Administrator hariç).
- `PATCH /organization/members/{userId}` `{ roleId?, isActive? }` → 204 (`org.users.manage`; `member.last_admin` kuralı; kendi rolünü değiştirme/kendini pasifleştirme `member.cannot_modify_self` 422; rolü/etkinleştirmeyi çağıranın izinleri kapsamıyorsa `role.permission_escalation` 403; bekleyen davet bu uçla değiştirilemez → 404)
- `GET /organization/roles` → `[{ id, name, isSystem, permissions, memberCount }]` (`org.users.read`)
- `POST /organization/roles` `{ name, permissions }` → 201 rol; `PUT /organization/roles/{id}` `{ name, permissions }` → 204; `DELETE /organization/roles/{id}` → 204 (`org.roles.manage`; bilinmeyen izin anahtarı → `validation`; `permissions` çağıranın izinlerinin alt kümesi olmalı, aksi 403 `role.permission_escalation` — Administrator sistem rolü hariç, C-SEC M7)
- `GET /organization/audit?page=1&pageSize=50` → `{ items: [{ id, entityType, entityId, action, userId?, userDisplayName?, changes, occurredAt }], total }` (`org.audit.read`), en yeni önce. `action`: `created|updated|deleted`; `changes`: `{ "<alan>": { "old": ..., "new": ... } }` (alan adları camelCase; oluşturmada `old: null`, silmede `new: null`).

### Sağlık ve dokümantasyon
`/health`, `/health/live`, `/health/ready` (Npgsql). `/openapi/v1.json` ve `/scalar` yalnız Development/Testing ortamında veya `Docs:Enabled=true` ile açılır (Production varsayılanı: kapalı).

### Platform yöneticisi (M5)
- `POST /platform/organizations` `{ organizationName, adminDisplayName, adminEmail, adminPassword|null, locale }` → 201 `{ organizationId, name, slug, adminUserId, adminEmail, adminAccountCreated, generatedPassword?, adminInvitationPending }` (C-SEC: **mevcut e-postada onaysız yönetici yapılmaz** — bekleyen Administrator daveti açılır, `adminInvitationPending: true`; yeni hesapta parola üretilmiş de olsa platform yöneticisince verilmiş de olsa geçicidir, `MustChangePassword = true`); `Cache-Control: no-store`. Yalnız `isPlatformAdmin` hesaplar (JWT bayrağı yetmez: handler kullanıcıyı ve bayrağı veritabanından doğrular; aksi `403 forbidden`). Yeni organizasyon + sistem rolleri + `OrganizationCreated` olayı (varsayılan huni) + Administrator üyeliği oluşur; yazma **yeni organizasyonun kiracı kapsamında** yapılır (platform yöneticisinin kendi kiracısı bağlamında başka kiracıya yazmak `AuditTenantInterceptor` tarafından reddedilirdi) ve denetim kaydı yeni organizasyona düşer. `adminPassword: null` → tek seferlik parola üretilir ve yalnız bu yanıtta döner; e-posta zaten bir hesapsa hesap değişmez, yalnız üyelik eklenir (`adminAccountCreated=false`).
- İlk platform yöneticisi HTTP ile değil Migrator komutuyla oluşur: `dotnet Sense.Crm.Migrator.dll create-platform-admin` (`PLATFORM_ADMIN_EMAIL`, `PLATFORM_ADMIN_PASSWORD` veya `PLATFORM_ADMIN_PASSWORD_FILE`, isteğe bağlı `PLATFORM_ADMIN_NAME`, `PLATFORM_ORG_NAME`; idempotent; parola ≥ 12 karakter; yeni hesap için "Platform" işletim organizasyonu açılır).

## 6. Kiracı izolasyonu ve denetim

- Kiracıya ait her varlık (`ITenantEntity`) için `ModuleDbContext` global `Tenant` query filter'ı ve `TenantId` indeksi ekler; `AuditTenantInterceptor` yazmada `TenantId`'yi atar, başka kiracıya yazmayı ve `TenantId` değiştirmeyi reddeder. Filtre atlama (`IgnoreQueryFilters`) **envanteri sabittir** (M7: `src/` altında 7 çağrı, 3 dosya — Identity `IdentityReadStore` ×3, `MembershipRepository` ×3 (yalnız çağıranın kendi üyelikleri/davetleri) ve Workflows `RunningExecutionSource` ×1 (durum senkronu için dar projeksiyon)); `Sense.Crm.Tests.TenantIsolation.TenantFilterBypassInventoryTests` listeyi kaynak taramasıyla birebir zorlar, ham SQL çağrılarının (`FromSql*`/`ExecuteSql*`) envanterini de tutar. Kiracı verisine çapraz erişim `ITenantContextSetter.BeginScope(tenantId)` ile yapılır (filtre atlanmaz).
- Küresel tablolar: `tenants`, `users`, `refresh_tokens` (yalnız hash ile bulunur), outbox/inbox.
- Denetim (`AuditLogInterceptor`): `IAuditLogged` agregatların oluşturma/güncelleme/silme işlemleri iş verisiyle aynı transaction'da `audit.audit_log_entries`'e yazılır; `SensitiveFields` maskelenir. Kayıt sırasında (anonim) `userId` boştur.
- Testler: `Sense.Crm.Tests.TenantIsolation` (model düzeyinde), `OrganizationApiTests.CrossTenantIsolation_*` (A yöneticisi B'nin üyelerini/rollerini/denetimini göremez, değiştiremez).

## 7. Açık işler
- Kayıt sahipliği bazlı görünürlük/rol hiyerarşisi (K7), organizasyon grubu ve konsolidasyon (K4), PostgreSQL RLS (K2 ikinci savunma hattı).
- E-posta ile davet ve parola **sıfırlama** (unutulan parola), MFA; SSO (K6). Uygulama içi davet ve geçici parola C-SEC ile geldi (§16).
- Üretim gözlemlenebilirliği (metrik/iz/merkezî log). Dockerfile'lar M5'te derlenip paketli yığında doğrulandı (bkz. §15).
- Workflows açık işleri: bkz. §13 sonu.
- Activities/rapor açık işleri: bağlı kaydı silinmiş aktivitelerin temizlik işi (ilişki bugün yumuşak: `relatedName` boş döner), tekrarlayan görevler, hatırlatma bildirimleri (bildirim altyapısı M4+); aktivite listesindeki `relatedName` bağlı kaydın türünü okuyamayan çağırana `***` yer tutucusu döner (C-SEC L2); raporlar çok para birimli tutarı ayırmadan toplar (M2 sınırlaması); rapor sorguları büyük kiracılar için henüz önbelleklenmez.
- Sales açık işleri: yinelenen firma/kişi tespiti, CSV içe aktarma, özel alanlar (kapsam dışı, bkz. [m2-satis-cekirdegi.md](../plan/m2-satis-cekirdegi.md)); pano `totalAmount` alanı para birimlerini ayırmadan toplar (TRY gösterimi; çok para birimli toplam sonraki iş); `GET /organization/members` hâlâ `org.users.read` ister (Administrator ve Standard'da var; bu izni taşımayan özel rollerde sahip seçici çalışmaz).

## 8. Sales modülü (Milestone 2 — satış çekirdeği)

HTTP sözleşmesinin bağlayıcı kaynağı [m2-api-kontrat.md](../plan/m2-api-kontrat.md); bu bölüm uygulanan hâli ve mimari kararları özetler.

**Modül:** `Sense.Crm.Modules.Sales.{Domain,Application,Contracts,Infrastructure,Api}`, şema `sales`, tek `SalesDbContext`. Firma, kişi ve fırsat lead dönüştürmede tek transaction'da yazıldığı için tek modüldedir.
Api host'u (`ModuleCatalog`), Migrator (`InitialSales`) ve Worker (outbox) Identity ile aynı kalıpla bağlanır.

**Agregatlar** (hepsi `TenantAggregateRoot`, `IAuditLogged`, yumuşak silinen; kimlikler `Guid.CreateVersion7`, EF'te `ValueGeneratedNever`):

| Agregat | Kurallar |
|---|---|
| `Account` | Ad kiracıda benzersiz değil; adres düz kolonlar (`BillingStreet…`; denetim alan bazında fark üretsin diye); `SensitiveFields`: e-posta, telefon |
| `Contact` | İsteğe bağlı firma; `SensitiveFields`: e-posta, telefon, cep; `FullName` türetilir |
| `Lead` | Kaynak `web\|referral\|campaign\|coldCall\|other`, durum `new\|contacted\|qualified\|unqualified\|converted`; `Convert(...)` sonrası salt-okur (`lead.already_converted`); durum `converted` yalnız dönüştürmeyle olur |
| `Pipeline` + `PipelineStage` | Aşama türü `open\|won\|lost`; tam bir `won` + bir `lost` + en az bir `open` şart; aşama sırası dizi sırasıdır; kaldırılan aşama yumuşak silinir; kullanımdaki (fırsatlı) aşama silinemez (`pipeline.stage_in_use`); organizasyonda tam bir varsayılan huni |
| `Deal` | Olasılık aşamadan gelir (saklanmaz); `MoveToStage` kazanma/kaybetmede `ClosedAt` yazar, açık aşamaya dönünce temizler, `lost` için neden zorunlu (`deal.lost_reason_required`), aşama değişince `DealStageChanged` domain event'i (outbox); tutar `decimal(18,4)`, para birimi ISO-4217 (varsayılan `TRY`) |

**Uygulama katmanı:** her komut/sorgu bir `[RequiresPermission]` taşır (kontratın izin tablosu): okuma `crm.<kaynak>.read`, yazma `crm.<kaynak>.write`, huni okuma `crm.deals.read`, huni değiştirme `org.settings.manage`; dönüştürme `leads.write + accounts.write + contacts.write` (+ fırsat açılıyorsa handler'da `crm.deals.write`). Doğrulama FluentValidation (`validation` + `errors`) ile yapılır ve yetkiden önce çalışır (geçersiz gövdeli yetkisiz istek 400 alır). Alan doğrulayıcıları oluşturma/güncelleme komutları arasında `I*Fields` arayüzü + genel taban doğrulayıcıyla paylaşılır.
- **Sahip (owner):** verilmezse çağıran kullanıcı; verilirse organizasyonun aktif üyesi olmalı (`owner.not_member`, 400) — `Identity.Contracts.IMemberLookup` (`IsActiveMemberAsync`, `GetDisplayNamesAsync`). Sahibi değişmeyen kayıtta üyelik yeniden sorgulanmaz (sahibi pasifleşen kayıt düzenlenebilir). Yanıtlarda `ownerName` bu arayüzle doldurulur (pasif üyeler dahil).
- **Liste sorguları:** `SalesReadStore` (kiracı + yumuşak silme filtresi altında). `q` `ILIKE` + kaçışlı parametredir (kullanıcı girdisindeki `%`/`_` düz metindir, SQL birleştirme yok); `sort` yalnız beyaz listedeki alanlarda (bilinmeyen alan yok sayılır, her zaman `Id` ile kararlı): firma `name, industry, createdAt, updatedAt`; kişi `lastName, firstName, email, createdAt, updatedAt`; lead `lastName, company, status, source, rating, createdAt, updatedAt`; fırsat `name, amount, closingDate, closedAt, createdAt, updatedAt`. Sayfalama varsayılan 25, üst sınır 100 (`Paging` ayarı; `PagingDefaults` de 25/100'e çekildi — `/organization/audit` de artık en çok 100 satır döner).
- **Pano:** `GET /deals/board` aşama başına tek toplama sorgusu (`count`, `totalAmount`) + aşama başına en çok 100 kart (en yeni önce).
- **Lead dönüştürme** (`ConvertLeadHandler`): firma (mevcut veya lead şirket adıyla yeni) + kişi + isteğe bağlı fırsat (pipeline'ın ilk açık aşaması) + lead durumu + `LeadConverted` outbox kaydı **tek `SaveChanges`/transaction**'da yazılır; tüm nesneler önce bellekte kurulur, herhangi bir kural/yazma hatasında hiçbiri kalıcı olmaz (entegrasyon testi fırsat yazımını patlatarak doğrular). Yeni firma ve kişinin sahibi lead'in sahibidir.
- **Olaylar:** `DealStageChanged` (domain event → Sales outbox), `LeadConverted` (integration event, `Sales.Contracts`; M3/M4 tüketir), `OrganizationCreated` (Identity integration event, `Identity.Contracts`; Sales tüketir).

**Varsayılan huni tohumlama (Identity, Sales'e bağlanmaz):** Kayıt (`SignUpHandler`) `OrganizationCreated` olayını aynı transaction'da Identity outbox'ına yazar; Worker olayı `IEventBus`'a yayınlar; Sales'in `OrganizationCreatedHandler`'ı `IDefaultPipelineSeeder` ile organizasyon dilinde (`tr|en`) 6 aşamalı varsayılan huniyi kurar. Aynı `DefaultPipelineSeeder` (organizasyon başına PostgreSQL advisory lock + kilit sonrası yeniden kontrol → idempotent, eşzamanlı çağrıya dayanıklı) üç yoldan çalışır: (1) Worker olayı, (2) API açılışında `DefaultPipelineSyncHostedService` — M1'de açılmış mevcut organizasyonlar için (`Identity.Contracts.ITenantDirectory`), (3) tembel güvence: Worker henüz olayı işlemediyse ilk huni/pano/fırsat isteği `DefaultPipelineResolver` ile tohumlar (kayıttan hemen sonraki istek de çalışır).

**Denetim:** Firma, kişi, lead, fırsat, huni ve huni aşaması değişiklikleri `audit.audit_log_entries`'e yazılır (aynı transaction; `EntityType` = `Account|Contact|Lead|Deal|Pipeline|PipelineStage`); kişisel veri alanları `***` ile maskelenir; enum alanları API'deki gibi camelCase string yazılır (`"converted"`). Kayıt bazlı uç: `GET /api/v1/audit?entityType=Account&entityId={id}&page&pageSize` → `/organization/audit` ile aynı `{ items, total }` biçimi (`Identity.Api.AuditController`). Yetki: `org.audit.read` **veya** varlık türünün kaynağının okuma izni; tür–izin eşlemesini her modül `Identity.Contracts.IAuditEntityPermissions` ile bildirir (Sales: `SalesAuditEntities`), bildirilmeyen türler yalnız `org.audit.read` ile okunur. `entityType`/`entityId` zorunludur (`validation`).

**Uç noktalar** (hepsi `/api/v1`, Bearer):

| Yol | Notlar |
|---|---|
| `GET/POST /accounts`, `GET/PUT/DELETE /accounts/{id}`, `GET /accounts/{id}/contacts`, `GET /accounts/{id}/deals` | Detayda `contactCount`, `dealCount`; bağlı kişi/fırsat varken silme `account.has_dependents` (409) |
| `GET/POST /contacts`, `GET/PUT/DELETE /contacts/{id}` | Filtre `accountId`, `ownerUserId`; yanıtta `fullName`, `accountName` |
| `GET/POST /leads`, `GET/PUT/DELETE /leads/{id}`, `POST /leads/{id}/convert` | Filtre `status`, `source`, `ownerUserId`; `convert` → 200 `{ accountId, contactId, dealId? }` |
| `GET/POST /pipelines`, `GET/PUT /pipelines/{id}`, `PUT /pipelines/{id}/stages` | Okuma `crm.deals.read`, değiştirme `org.settings.manage`; varsayılanı düşürme `pipeline.default_required` (422) |
| `GET/POST /deals`, `GET/PUT/DELETE /deals/{id}`, `GET /deals/board`, `POST /deals/{id}/stage` | Filtre `pipelineId, stageId, stageKind, ownerUserId, accountId`; `PUT` aşamayı değiştirmez; yanıtta `contactId?`, `contactName?`, `stageName`, `stageKind`, `probability`, `pipelineName`, `closedAt?`, `lostReason?` |
| `GET /audit?entityType&entityId` | Kayıt bazlı denetim (yukarıda) |

Oluşturma uçları 201 + `Location` + oluşan kaydın gövdesini döner; güncelleme/silme 204 (PUT tam değiştirmedir: gönderilmeyen isteğe bağlı alan temizlenir, `ownerUserId` verilmezse mevcut sahip korunur).

**Yeni hata kodları** (kontrat + HTTP eşlemeleri; metinler `SharedResource.resx` tr/en):

| code | HTTP | Ne zaman |
|---|---|---|
| `lead.already_converted` | 409 | Dönüşmüş lead güncelleme/yeniden dönüştürme |
| `account.has_dependents` | 409 | Bağlı kişi/fırsatı olan firmayı silme |
| `pipeline.stage_in_use` | 409 | Fırsat içeren aşamayı silme (`stageName` argümanı) |
| `pipeline.default_required` | 422 | Varsayılan huniyi "varsayılan değil" yapma |
| `pipeline.stage_not_found` | 404 | Aşama bu hunide/organizasyonda yok |
| `deal.lost_reason_required` | 400 | `lost` aşamaya kayıp nedeni olmadan geçiş |
| `owner.not_member` | 400 | Sahip organizasyonun aktif üyesi değil |

Başka organizasyonun kaydı her zaman `not_found` (404) döner (çapraz referanslar dahil: başka organizasyonun firmasına kişi/fırsat bağlamak, başka organizasyonun aşamasına taşımak).

**Testler:** `Sense.Crm.Modules.Sales.Tests` — domain birim testleri (lead dönüştürme/değişmezlik, aşama/olasılık, kayıp nedeni, tam bir won + bir lost kuralı, aşama yeniden düzenleme/kullanımdaki aşama, hassas alanlar); Testcontainers HTTP testleri (her kaynağın CRUD'u, dönüştürme + geri alma + gerçek atomiklik, pano toplamları/100 kart sınırı, liste filtre/sıralama/sayfalama/arama kaçışı, yazma izni olmayan kullanıcıya 403, sahip kuralı, **kiracılar arası izolasyon**, kayıtta huni tohumlama (tr/en), açılışta mevcut organizasyon tohumlama, denetim kayıtları + maskeleme + kayıt bazlı uç). Mimari ve kiracı izolasyonu testleri yeni modülü otomatik kapsar.

## 9. Milestone 2'de değişenler (backend)
- Yeni modül `Sales` (şema `sales`, migration `InitialSales`); `Sense.Crm.Api`, `Sense.Crm.Migrator`, `Sense.Crm.Worker`'a bağlandı.
- `Identity.Contracts`: `CrmPermissions` içinden `accounts/contacts/leads/deals` anahtarları `SalesPermissions`'a taşındı; `IMemberLookup.GetDisplayNamesAsync`, `ITenantDirectory`, `OrganizationCreated`, `IAuditEntityPermissions` eklendi. Sistem rolleri: Administrator tüm anahtarlar (16), Standard tüm `crm.*` + `org.users.read`; mevcut organizasyonlar API açılışında `SystemRolePermissionSynchronizer` ile güncellenir.
- Identity: kayıtta `OrganizationCreated` outbox olayı; `GET /audit?entityType&entityId` (kayıt bazlı denetim).
- Ortak: `Paging` varsayılanı 25 / üst sınır 100; `AuditLogInterceptor` enum alanlarını camelCase string yazar; `SharedResource` tr/en'e Sales hata/alan anahtarları eklendi.
- Test altyapısı: `CrmApiFactory` Respawn şemalarına `sales` eklendi.

## 10. Activities modülü (Milestone 3)

HTTP sözleşmesinin bağlayıcı kaynağı [m3-aktivite-rapor.md](../plan/m3-aktivite-rapor.md); bu bölüm uygulanan hâli ve kararları özetler.

**Modül:** `Sense.Crm.Modules.Activities.{Domain,Application,Contracts,Infrastructure,Api}`, şema `activities`, tek `ActivitiesDbContext` (migration `InitialActivities`). `Sense.Crm.Api` (`ModuleCatalog`), `Sense.Crm.Migrator` ve `Sense.Crm.Worker` (outbox; bugün olay üretmez) Sales ile aynı kalıpla bağlandı. Modül Sales'e yalnız `Sales.Contracts` üzerinden, Identity'ye `Identity.Contracts` üzerinden bağlıdır (mimari testler yeşil).

**Agregat `Activity`** (`TenantAggregateRoot`, `IAuditLogged`, yumuşak silinen; kimlik `Guid.CreateVersion7`, kişisel veri alanı yok → maskelenen alan yok):

| Kural | Uygulama |
|---|---|
| Tür | `task`, `call`, `meeting`, `note`; `PUT` türü değiştirebilir (nota çevirmek durumu `completed` yapar) |
| Durum | `open`, `completed`, `cancelled`; oluştururken verilmezse `open` (not: `completed`). Geçişler: tamamla (`completedAt` yazılır, tekrar tamamlama ilk anı korur), yeniden aç (`completedAt` temizlenir; tamamlanmış ve iptal edilmişten), `PUT` ile `status` |
| Not | Her zaman `completed`; `complete`/`reopen`, farklı `status` (POST/PUT) → `activity.note_status_fixed` (409) |
| Aralık | `endAt >= startAt` (ikisi de verilmişse), aksi `activity.invalid_range` (400); zamanlar UTC'ye normalleştirilir (`Kind` belirtilmemiş = UTC) |
| Öncelik | `low`, `normal`, `high`; varsayılan `normal` (PUT'ta verilmezse `normal`) |
| İlişki | `relatedType` (`account`, `contact`, `lead`, `deal`) + `relatedId` birlikte (yoksa `validation`); yumuşak bağ: bağlı kayıt silinirse aktivite kalır, `relatedName` boş döner ve aktivite başka alanlar için düzenlenebilir kalır (ilişki yalnız değiştiyse yeniden doğrulanır) |
| Atanan | Verilmezse çağıran; verilen aktif üye değilse `owner.not_member` (400) — `IMemberLookup`; mevcut atanan korunurken üyelik yeniden sorgulanmaz |
| `isOverdue` | Yanıtta hesaplanır: `open` ve `dueAt` geçmiş |

**Sales'e bağ (`Sales.Contracts.IRecordLookup`):** `ExistsAsync`, `GetDisplayNameAsync`, toplu `GetDisplayNamesAsync(RecordRef[])` (tür başına tek sorgu). `Sales.Infrastructure.RecordLookup` kiracı + yumuşak silme filtresi altında uygular; ad: firma adı, kişi/potansiyel tam adı, fırsat adı. Yok / silinmiş / başka organizasyon → `activity.related_not_found` (404; M2'deki çapraz referans davranışıyla aynı, varlık sızdırılmaz).

**Uç noktalar** (`/api/v1`, izin `crm.activities.read` / `crm.activities.write`):

| Yol | Notlar |
|---|---|
| `GET /activities` | Filtre: `q` (konu + açıklama, `ILIKE`, joker kaçışlı), `type`, `status`, `assignedUserId`, `relatedType`, `relatedId`, `dueFrom`, `dueTo`, `overdue`; `sort`: `dueAt` (varsayılan artan; **boş `dueAt` her iki yönde de sonda**), `createdAt`, `subject`, `priority` (low < normal < high); bilinmeyen alan yok sayılır; her zaman `createdAt` + `Id` ile kararlı; sayfalama varsayılan 25 / üst sınır 100 |
| `GET /activities/summary?assignedUserId` | Yoksa çağıran → `{ openCount, overdueCount, dueTodayCount, completedThisWeek }` |
| `GET /activities/{id}`, `POST` (201), `PUT /{id}` (204, tam değiştirme), `DELETE /{id}` (204, yumuşak) | POST/PUT gövdesi: `type*, subject*, description?, status?, priority?, dueAt?, startAt?, endAt?, relatedType?, relatedId?, assignedUserId?` |
| `POST /activities/{id}/complete`, `POST /activities/{id}/reopen` | 204; idempotent; not için 409 |
| `GET /reports/activities/by-user?from&to` | `crm.reports.read` (aşağıda) |

`dueFrom`/`dueTo` UTC anlarıdır ve **uçları dahildir** (`dueAt >= dueFrom`, `dueAt <= dueTo`); biri verildiğinde `dueAt`'ı olmayan aktiviteler dışarıda kalır. Bu sayede pano listesi (`type=task&status=open&assignedUserId=<ben>&dueTo=<bugünün son anı>&sort=dueAt`) geciken + bugün vadeli görevleri en eski önce getirir. `overdue=true` yalnız `open` ve `dueAt` geçmiş olanları, `overdue=false` bunların dışındakileri döner. Sorgu parametresindeki tarih-saatlerin `Kind`'ı yoksa UTC kabul edilir.

**Özet ve kiracı saat dilimi:** "bugün" ve "bu hafta" **organizasyonun saat diliminde** (`organization.timeZone`) hesaplanır: bugün = yerel gün `[00:00, ertesi 00:00)`, hafta = pazartesi başlangıçlı `[pzt 00:00, sonraki pzt 00:00)`; sınırlar UTC'ye çevrilip sorguya gider (yaz saati geçişi ve UTC'den çok farklı dilimler testlidir). `openCount` açık, `overdueCount` açık ve `dueAt < şimdi`, `dueTodayCount` açık ve `dueAt` bugünün içinde (geçmiş olsun olmasın), `completedThisWeek` bu hafta tamamlanan. **Notlar (her zaman tamamlanmış) özet ve aktivite raporunda sayılmaz; iptal edilenler `openCount`'a girmez.** Saat dilimi `Identity.Contracts.TenantInfo.TimeZone` (`ITenantDirectory`) ile okunur; `TenantCalendarService` (Identity.Contracts, Identity kaydeder) Sales ve Activities için ortak kısayoldur (`Kernel.Time.TenantCalendar`: bugün, hafta başı, gün başlangıcı, UTC sınırları, ISO hafta/ay etiketleri; bilinmeyen kimlik → UTC).

**Denetim:** `Activity` değişiklikleri `audit.audit_log_entries`'e yazılır (`EntityType` = `Activity`); enum alanları camelCase string. `GET /audit?entityType=Activity&entityId=` **`crm.activities.read`** ile çalışır: Activities modülü `IAuditEntityPermissions` (`ActivitiesAuditEntityPermissions`, `ActivitiesAuditEntities`) ile türü bildirir; Identity'deki `GetEntityAuditHandler` değişmedi.

**Yeni hata kodları** (metinler `SharedResource.resx` tr/en):

| code | HTTP | Ne zaman |
|---|---|---|
| `activity.related_not_found` | 404 | İlişkili kayıt aktif organizasyonda yok |
| `activity.invalid_range` | 400 | `endAt < startAt` |
| `activity.note_status_fixed` | 409 | Not için tamamla/yeniden aç veya farklı durum |
| `owner.not_member` | 400 | Atanan aktif üye değil (Sales ile aynı kod) |

## 11. Raporlar (Milestone 3)

Hepsi `crm.reports.read` ister. Tarih parametreleri `from`, `to` (`YYYY-MM-DD`, **uçlar dahil**) **organizasyon saat diliminde takvim günü** olarak yorumlanır (UTC değil; ör. Europe/Istanbul'da 31 Mart 21:30Z, 1 Nisan sayılır); ikisi de isteğe bağlıdır, varsayılan **son 12 ay**: `to` = bugün (kiracı saatiyle), `from` = 11 ay önceki ayın ilk günü (ay bazlı gruplamada tam 12 dönem). Ters aralık veya 10 yılı aşan aralık → `validation` (`errors.to`); yalnız `from` verilip bugünden sonraya düşerse `validation`. Tutarlar fırsat para birimlerine bakmadan toplanır (`currency` yok).

| Yol | Modül | Yanıt ve tanım |
|---|---|---|
| `GET /reports/sales/funnel?pipelineId` | Sales | `{ pipelineId, stages: [{ id, name, kind, order, probability, count, totalAmount }] }` — huni verilmezse varsayılan (yoksa tohumlanır); **güncel durum** (tarih aralığı yok), tüm aşamalar sırasıyla (boş aşama 0), silinmiş fırsatlar yok; bilinmeyen/başka organizasyon huni `not_found` |
| `GET /reports/sales/won-lost?from&to&groupBy` | Sales | `groupBy` = `month` (varsayılan) veya `week`. `[{ period, wonCount, wonAmount, lostCount, lostAmount }]` — `closedAt`'a göre; dönem `2026-09` veya ISO hafta `2026-W38`; **aralıktaki boş dönemler 0 ile doldurulur**; ilk/son dönem aralık kenarında kısmi olabilir; artan sıra |
| `GET /reports/sales/leads-by-source?from&to` | Sales | `[{ source, count, convertedCount }]` — `createdAt`'a göre; yalnız potansiyeli olan kaynaklar; sayı azalan, sonra kaynak sırası |
| `GET /reports/sales/by-owner?from&to` | Sales | `[{ ownerUserId, ownerName, openDealCount, openDealAmount, wonCount, wonAmount, leadCount }]` — **açık fırsatlar güncel durumdur (aralıktan bağımsız)**, `won*` aralıktaki `closedAt`, `leadCount` aralıktaki `createdAt`; kazanılan tutar azalan sıra; adlar `IMemberLookup` (pasif üyeler dahil) |
| `GET /reports/activities/by-user?from&to` | Activities | `[{ userId, userName, completedCount, openCount, overdueCount }]` — `completedCount`: aralıkta `completedAt`; `openCount`: `open` ve `dueAt` aralıkta; `overdueCount`: bunlardan `dueAt < şimdi`; notlar hariç; tamamlanan azalan sıra |

Uygulama: Sales'te `ISalesReportStore` (`SalesReportStore`, veritabanında toplama; kapanış günleri `timezone(zone, closed_at)` ile — `SalesDbFunctions.ToLocalTimestamp` DbFunction eşlemesi — yerel güne indirilir) + `WonLostPeriods` (ay/ISO hafta bölme ve sıfır doldurma, saf hesap, birim testli); Activities'te `IActivityReadStore.GetUserTotalsAsync`. Rapor handler'ları `TenantCalendarService.ResolveReportRangeAsync` ile aralığı UTC yarı açık `[from, to+1 gün)` sınırlarına çevirir.

**Testler:** `Sense.Crm.Shared.Kernel.Tests` (`TenantCalendar`: saat dilimi, yaz saati, pazartesi haftası, ISO etiketleri, varsayılan aralık); `Sense.Crm.Modules.Activities.Tests` — domain (durum geçişleri, aralık kuralı, not kuralı, UTC normalleştirme, `isOverdue`, özet penceresi) + Testcontainers HTTP (CRUD ve tam değiştirme, doğrulama, tür kuralları, tamamla/yeniden aç, ilişkili kayıt doğrulama/adı/yumuşak bağ, atama kuralı, filtre/sıralama/sayfalama, **özet saat dilimi sınırları (UTC+14 kiracı)**, izinler, **kiracı izolasyonu**, denetim + `crm.activities.read` ile kayıt bazlı okuma, aktivite raporu bilinen veriyle); `Sense.Crm.Modules.Sales.Tests` — `SalesReportsApiTests` (funnel/won-lost/kaynak/temsilci bilinen veriyle, aralık uçları kiracı saat diliminde, boş dönem doldurma, ISO hafta, varsayılan aralık, izin, doğrulama, kiracı izolasyonu) + `WonLostPeriodsTests`.

## 12. Milestone 3'te değişenler (backend)
- Yeni modül `Activities` (şema `activities`, migration `InitialActivities`); `Sense.Crm.Api`, `Sense.Crm.Migrator`, `Sense.Crm.Worker`'a bağlandı; test altyapısı Respawn şemalarına `activities` eklendi; yeni test projesi `Sense.Crm.Modules.Activities.Tests`.
- `Sales.Contracts`: `IRecordLookup` (+ `RecordType`, `RecordRef`). `Sales`: 4 satış raporu ucu, `ISalesReportStore`, `SalesDbFunctions`.
- `Identity.Contracts`: `crm.activities.*` anahtarları `ActivitiesPermissions`'a taşındı (dizgeler aynı; Administrator = tüm 16 anahtar, Standard = tüm `crm.*` + `org.users.read` kuralı korunur — `SystemRolePermissionSynchronizer` API açılışında mevcut organizasyonlara da uygular); `CrmPermissions` yalnız `crm.reports.read` taşır; `TenantInfo.TimeZone` ve `TenantCalendarService` eklendi.
- `Kernel`: `Time.TenantCalendar`.
- `SharedResource` tr/en: `activity.*` hata anahtarları, `validation.activity_related`, `validation.date_range`, yeni `field.*` adları.

## 13. Workflows modülü (Milestone 4 — Conductor OSS)

HTTP sözleşmesinin bağlayıcı kaynağı [m4-workflow.md](../plan/m4-workflow.md); bu bölüm uygulanan hâli, Conductor kurulumunu ve olay akışını anlatır.

**Modül:** `Sense.Crm.Modules.Workflows.{Domain,Application,Contracts,Infrastructure,Api}`, şema `workflows`, tek `WorkflowsDbContext` (migration `InitialWorkflows`). Sales/Activities/Identity'ye yalnız `*.Contracts` üzerinden bağlıdır (mimari testler yeşil).

**Agregatlar** (hepsi `TenantAggregateRoot`, `IAuditLogged`; kimlikler `Guid.CreateVersion7`):

| Agregat / tablo | Kurallar |
|---|---|
| `WorkflowRule` / `workflow_rules` (yumuşak silinir) | `kind` = `leadAssignment` veya `dealApproval`; `params` tür şemasına göre doğrulanır ve `jsonb` saklanır. `leadAssignment`: `{ sources?, assigneeRoleId, followUpHours (1–720, varsayılan 24) }`, `dealApproval`: `{ minAmount (>0), approverRoleId }`. Hatalar `params.<alan>` altında (`RuleParamsParser`, birim testli); rol kiracıda yoksa `workflow.role_not_found` (400). POST'ta `isEnabled` verilmezse etkin başlar; PUT tür + parametreleri tümden değiştirir, etkin durumu `enable`/`disable` uçlarıyla değişir. Aynı türden birden çok etkin kural hepsi çalışır. Kural değişiklikleri denetim kaydına yazılır (`GET /audit?entityType=WorkflowRule&entityId=`, `org.workflows.manage`) |
| `WorkflowExecution` / `workflow_executions` | Kural adı/türü/konu adı anlık görüntüdür (kural silinse geçmiş okunur). `status`: `running`, sonra `completed`, `failed` veya `terminated` (bir kez sonlanınca değişmez); `error` (kararlı kod: `no_assignee`, `no_approver`, `workflow.engine_unavailable`, `engine_workflow_not_found` ya da motorun ham nedeni). İdempotency: `(tenant, rule, trigger_event_id, attempt)` benzersiz indeks; "yeniden dene" aynı olay için bir sonraki `attempt`'i (yeni yürütme) açar |
| `Approval` / `approvals` | `pending`, sonra `approved`, `rejected` veya `cancelled`; reddetmede yorum zorunlu (`validation`, alan `comment`); karar verilmiş/iptal edilen talep değişmez (`approval.already_decided` 409); `(execution, approver)` benzersiz. Tek onay yeter: **ilk karar** (onay ya da red) sonucu belirler, diğer bekleyen talepler aynı transaction'da `cancelled` olur. Başlık/konu adı/tutar anlık görüntüdür |

**Uç noktalar** (hepsi `/api/v1`, Bearer): `GET/POST /workflows/rules` (liste **düz dizi**, oluşturma 201 + gövde), `GET/PUT/DELETE /workflows/rules/{id}`, `POST .../enable` ve `.../disable`, `GET /workflows/executions?status&ruleId&from&to&subjectType&subjectId&page&pageSize` (en yeni önce; `from`/`to` UTC anları, uçlar dahil; `subjectType`/`subjectId` lead/fırsat detayındaki durum şeridi içindir), `GET /workflows/executions/{id}` (özet + `steps` Conductor'dan ham durum metniyle `COMPLETED`, `IN_PROGRESS`, `FAILED_WITH_TERMINAL_ERROR`… + varsa `approvals`; motora ulaşılamazsa `steps` boş), `POST .../terminate` (yalnız running, aksi `workflow.not_running` 409; bekleyen onaylar iptal), `POST .../retry` (yalnız failed, aksi `workflow.not_failed` 409; kural silinmişse 404; güncel kural parametrelerini kullanır), `GET /approvals?status&mine&page&pageSize` (`mine` varsayılan `true`, ek izin gerekmez; `mine=false` `org.workflows.manage` ister), `GET /approvals/{id}` (kendi onayı ya da `org.workflows.manage`, aksi 403), `POST /approvals/{id}/decision` (`{ decision: "approve" veya "reject", comment? }`, `crm.approvals.decide` + onay çağırana ait olmalı), `GET /approvals/summary` (`{ pendingCount }`, kimliği doğrulanmış **her** üye için 200). Kural/yürütme uçları `org.workflows.manage` ister.

**Yeni hata kodları** (metinler `SharedResource.resx` tr/en): `workflow.role_not_found` (400), `workflow.not_running` (409), `workflow.not_failed` (409), `workflow.engine_unavailable` (500), `approval.already_decided` (409), `approval.comment_required`, yürütme hata kodları `no_assignee`, `no_approver`, `engine_workflow_not_found`; doğrulama mesajları `validation.workflow_params`, `validation.workflow_source`, `validation.follow_up_hours`, `validation.min_amount`, `validation.workflow_role_id`. İzin adları istemcide çevrilir (K8); `permission.*` anahtarları da resx'e eklendi.

### Conductor OSS kurulumu (`infra/docker-compose.yml`, `infra/conductor/config-postgres.properties`)
- **İmaj/sürüm:** `conductoross/conductor:3.32.4` (2026-09 itibarıyla son kararlı; `latest` kullanılmaz). İmaj sunucu + UI'yı tek konteynerde taşır, host portu **18090** (`CONDUCTOR_PORT` ile değişir): UI `/`, REST `/api`, sağlık `/health`.
- **Kalıcılık: PostgreSQL, Redis/Elasticsearch yok.** `conductor.db.type`, `conductor.queue.type` ve `conductor.indexing.type` = `postgres` + `conductor.elasticsearch.version=0`; veri `crm-postgres` içindeki ayrı `conductor` veritabanındadır (tek seferlik `conductor-db-init` servisi veritabanını idempotent oluşturur — postgres `init.sql` yalnız ilk kurulumda çalıştığı için mevcut hacimlerde de çalışır). Gömülü SQLite varsayılanı yerine bunun seçilme nedeni: eşzamanlı poll/güncelleme yükü ve yeniden başlatma dayanıklılığı; ek servis maliyeti sıfır (mevcut postgres). Bağlantı bilgisi ortam değişkeninden gelir (`CONDUCTOR_DB_USER`/`CONDUCTOR_DB_PASSWORD`, yalnız yerel geliştirme parolası).
- **Başlatma:** `POSTGRES_PORT=15433 docker compose -f infra/docker-compose.yml up -d --no-deps conductor-db-init conductor` (bu makinede postgres host portu 15433'tür; `--no-deps` mevcut postgres konteynerini yeniden yaratmaz).
- **Uygulama ayarı:** `Conductor:BaseUrl` (Api ve Worker; Development'ta `http://localhost:18090`, boşsa motor yapılandırılmamış sayılır: başlatmalar `failed`/`workflow.engine_unavailable` olur, süreçler çalışmaya devam eder), `Conductor:PollBatchSize`, `PollTimeoutMs`, `StatusSyncIntervalSeconds`, `WorkerId`.
- **Doğrulanan REST uçları:** tanım kaydı `PUT /api/metadata/workflow` (dizi, idempotent üzerine yazar), görev tanımı `PUT` veya `POST /api/metadata/taskdefs` (PUT yoksa 404 döner → istemci POST'a düşer), başlat `POST /api/workflow`, oku `GET /api/workflow/{id}?includeTasks=true`, sonlandır `DELETE /api/workflow/{id}`, poll `GET /api/tasks/poll/batch/{tip}`, sonuç `POST /api/tasks`, HUMAN görevini tamamla `POST /api/tasks/{workflowId}/{taskRefName}/COMPLETED`. Conductor'un `idempotencyKey` alanı bu sürümde işe yaramadığından idempotency CRM tarafında (benzersiz indeks) sağlanır.

### Motor portu ve tanımlar
- `IWorkflowEngine` (Application): `StartAsync`, `GetAsync`, `TerminateAsync`, `CompleteWaitTaskAsync`. Uygulaması `ConductorWorkflowEngine` (tipli `HttpClient` + `Microsoft.Extensions.Http.Resilience` standart işleyicisi; yeniden deneme yalnız güvenli yöntemlerde). Testlerde `Sense.Crm.Tests.Shared.Workflows.FakeWorkflowEngine`: gerçek tanım JSON'larını ve gerçek görev işleyicilerini çalıştıran sahte motor (kesinti simülasyonu bayrakları).
- **Tanımlar kodda, sürümlü:** `Infrastructure/Definitions/crm_lead_assignment.v1.json`, `crm_deal_approval.v1.json`, `task_definitions.v1.json` (gömülü kaynak). API ve Worker başlangıcında arka planda `PUT` ile idempotent kaydedilir (`WorkflowDefinitionRegistrationService`; Conductor henüz ayakta değilse 6 kez dener, ilk workflow başlatmasında da `EnsureRegisteredAsync` yeniden dener). Tanım değişirse yeni sürüm dosyası eklenir ve `WorkflowNames.DefinitionVersion` artırılır. Görev tanımları ortak olarak 3 yeniden deneme + üstel geri çekilme + 120 sn yanıt zaman aşımıdır.
- **Workflow 1 `crm_lead_assignment`:** `crm_assign_lead_owner` → `crm_create_followup_task`. **Workflow 2 `crm_deal_approval`:** `crm_create_approvals` → HUMAN `wait_for_decision` (ref `wait_decision`) → `crm_record_decision` → `crm_cancel_pending_approvals`.

### Olay akışı
1. **Lead:** `CreateLeadHandler` `LeadCreated` integration olayını (`Sales.Contracts`) aynı transaction'da Sales outbox'ına yazar. **Fırsat:** mevcut `DealStageChanged` domain olayı Worker'da `DealStageChangedIntegrationPublisher` (Sales.Infrastructure) ile `DealStageChangedIntegration`'a çevrilip outbox'a eklenir; olay kimliği domain olay kimliğinden deterministik türetilir (yeniden işlemede aynı kimlik). Mevcut Sales davranışı değişmedi.
2. Worker'ın `OutboxPollingService<SalesDbContext>`'i olayı `IEventBus`'a yayınlar; **Worker'da** kayıtlı `LeadCreatedWorkflowHandler` / `DealStageChangedWorkflowHandler` (`WorkflowTrigger`) kiracının **etkin kurallarını** okur: lead için kaynak filtresi; fırsat için aşama türü `won` ve `amount >= minAmount`.
3. Uyan her kural için `workflow_executions` satırı **önce** yazılır (idempotency anahtarı `ruleId + eventId`), sonra motor başlatılır ve `engine_workflow_id` işlenir. Olay yeniden teslim edilirse ikinci yürütme/workflow açılmaz. Motor hatasında yürütme `failed` (`workflow.engine_unavailable`) olur ve arayüzden yeniden denenebilir.
4. **Görev işleyicileri** (`Sense.Crm.Worker/Workflows/ConductorTaskPollingService`): her görev türünü toplu poll eder, `WorkflowTaskRunner` ile sistem bağlamı + görev başına DI kapsamında çalıştırır. **Görev girdisi güvenilmezdir (C-SEC H1):** `input.tenantId`/`input.executionId` yalnız arama anahtarıdır; görev çalışmadan önce `(tenantId, executionId, Conductor'un verdiği workflow örnek kimliği)` üçlüsü `workflow_executions` ile doğrulanır (§16.1); iş kuralı hatası `FAILED_WITH_TERMINAL_ERROR` (yeniden denenmez, neden kararlı kod), geçici hata `FAILED` (motor yeniden dener). Görevler bir Worker'da sıralı işlenir (round-robin yarışsız). Conductor at-least-once teslim eder; onay kaydı `(execution, approver)` benzersiz olduğundan tekrar güvenlidir.
   - `crm_assign_lead_owner`: `IRoleMemberLookup` (Identity.Contracts) ile rolün aktif üyeleri; `ILeadOwnerService` (Sales.Contracts) ile açık lead sayıları ve son atanma zamanı; `RoundRobinSelector`: en az açık lead, eşitlikte en eski atama (hiç atanmamış önce), sonra kimlik. Rolde aktif üye yoksa `no_assignee` (lead sahibi değişmez). Atama `Lead.AssignOwner` (yeni `owner_assigned_at` kolonu) ile yapılır.
   - `crm_create_followup_task`, `crm_create_approvals`, `crm_record_decision`: `IActivityCreator` (Activities.Contracts) ile "Yeni potansiyel: <ad>" görevi (`dueAt = şimdi + followUpHours`), onaylayıcı başına "Fırsat onayı: <ad>" görevi + `approvals` kaydı, karar notu "Onay: onaylandı/reddedildi (yorum)" (kararı veren kullanıcıya atanmış `note`). Metinler organizasyonun dilinde (tr/en). Rolde onaylayıcı yoksa `no_approver`.
   - `crm_cancel_pending_approvals`: güvenlik ağı (karar ucu diğer talepleri zaten iptal eder).
5. **Karar:** `POST /approvals/{id}/decision` onayı kaydeder, kardeş onayları iptal eder ve workflow'un HUMAN görevini **API içinden** (`IWorkflowEngine.CompleteWaitTaskAsync`) tamamlar; motor reddederse hiçbir şey kaydedilmez (500 `workflow.engine_unavailable`, tekrar denenebilir). HUMAN görevi henüz zamanlanmadıysa motor istemcisi kısa süre bekler; görev zaten tamamlanmışsa idempotent başarıdır.
6. **Durum senkronu:** Worker'daki `ExecutionStatusSyncService` her 5 sn çalışan yürütmeleri Conductor'dan okur (`ExecutionStatusSynchronizer`): `running` → `completed`, `failed` veya `terminated`; hata nedeni önce başarısız görevin `reasonForIncompletion`'ından alınır; başarısız/sonlanan yürütmenin bekleyen onayları iptal edilir; motora hiç iletilememiş yürütme 2 dk sonra `failed` olur; motorda bulunamayan workflow `failed` (`engine_workflow_not_found`) olur. **Kiracılar arası tek okuma** `RunningExecutionSource`'tur (`IgnoreQueryFilters`, yalnız `(kiracı, yürütme)` projeksiyonu; §6'daki iki bilinçli filtre atlamaya üçüncü olarak eklenmiştir); her yürütme sonra kendi kiracı kapsamında işlenir.

**Cross-module Contracts (yeni):** `Sales.Contracts`: `LeadCreated`, `DealStageChangedIntegration`, `DealStageKinds`, `ILeadOwnerService`. `Activities.Contracts`: `IActivityCreator`. `Identity.Contracts`: `IRoleMemberLookup`, `RoleMember`. Uygulamalar Worker'da da gerekli olduğundan her modülde `Add<Modül>ContractServices()` uzantısıyla toplanır (Worker Application assembly'lerini taramaz).

**Testler:** `Sense.Crm.Modules.Workflows.Tests` — birim (parametre doğrulaması, round-robin seçimi, agregat kuralları, Conductor JSON eşlemesi, tanım/görev katalog tutarlılığı) + Testcontainers HTTP (sahte motor + gerçek tanımlar/işleyiciler): kural CRUD/doğrulama/izin/denetim/kiracı izolasyonu; lead atama uçtan uca (round-robin, en az açık lead, kaynak filtresi, idempotent tetikleme, `no_assignee` + retry, pasif üyeler, motor kesintisi); fırsat onayı uçtan uca (onay + iptal + not + tamamlandı, red yorumu zorunlu, eşik/aşama koşulları, terminate, `no_approver`, karar sırasında motor kesintisi, izinler, kiracı izolasyonu, olay yayını). **Canlı duman testi:** `build/smoke-workflows.ps1` (gerçek Conductor konteyneri + API + Worker; iki workflow + hata yolu + terminate).

## 14. Milestone 4'te değişenler (backend)
- Yeni modül `Workflows` (şema `workflows`, migration `InitialWorkflows`: `workflow_rules`, `workflow_executions`, `approvals`; indeksler kiracı ön ekli: yürütme `(tenant, status, started_at)`, `(tenant, rule, trigger_event, attempt)` benzersiz, `(tenant, subject)`; onay `(tenant, approver, status)`, `(tenant, execution, approver)` benzersiz; kural `(tenant, kind, is_enabled)`); `Sense.Crm.Api` (`ModuleCatalog`), `Sense.Crm.Migrator`, `Sense.Crm.Worker`'a bağlandı; yeni test projesi `Sense.Crm.Modules.Workflows.Tests`; `CrmApiFactory` Respawn şemalarına `workflows` eklendi ve tüm testlerde sahte workflow motoruyla çalışır.
- `Sales`: `LeadCreated` (lead oluşturulunca outbox), `DealStageChangedIntegration` (mevcut domain olayından), `ILeadOwnerService` (+ `LeadOwnerService`), `Lead.OwnerAssignedAt` + `AssignOwner` (migration `LeadOwnerAssignedAt`; elle sahip değişikliği de atama anını günceller). Mevcut davranış değişmedi.
- `Activities`: `IActivityCreator` (+ `ActivityCreator`; aynı kurallar: aktif üye, ilişkili kayıt var). `Identity`: `IRoleMemberLookup` (+ `RoleMemberLookup`); yeni izinler `org.workflows.manage`, `crm.approvals.decide` (Administrator tümünü alır, Standard almaz; mevcut organizasyonlar API açılışında `SystemRolePermissionSynchronizer` ile senkronlanır); `GET /permissions` 18 anahtar.
- Altyapı: `infra/docker-compose.yml`'a Conductor OSS (+ `conductor-db-init`), `infra/conductor/config-postgres.properties`; `Directory.Packages.props`'a `Microsoft.Extensions.Http.Resilience`; `appsettings`'e `Conductor` bölümü (Api + Worker) ve `Polly`/`HttpClient` log seviyesi.
- **Açık işler:** çok adımlı/sıralı onay zincirleri, onayın aşama geçişini engellemesi, zamanlanmış tetikleyiciler, e-posta/SMS bildirimi, Conductor kimlik doğrulaması/çok kiracılı Conductor (kapsam dışı, bkz. plan); onay verilince onaylayıcının açık "Fırsat onayı" **görevi** kendiliğinden tamamlanmaz (kullanıcı tamamlar); görev işleyicileri at-least-once (yalnız onay kaydı idempotent; çökme anında takip görevi/not ikilenebilir); çok Worker örneğinde round-robin adaleti küçük sapabilir (advisory lock yok); durum senkronu poll tabanlıdır (webhook/`workflowStatusListener` yok, gecikme en çok ~5 sn); Conductor üretim kurulumu (Redis/Elasticsearch, kimlik doğrulama, Helm) K13 kapsamındadır.

## 15. Milestone 5'te değişenler (pilot yayın)

Ayrıntı: [m5-pilot-yayin.md](../plan/m5-pilot-yayin.md); işletim: [runbook.md](../operations/runbook.md).
- **Identity:** `Registration:Mode` (`RegistrationOptions`, `IRegistrationPolicy`; boş = Development/Testing açık, diğerleri kapalı), `GET /auth/config`, `auth.signup_disabled`; `OrganizationProvisioner` (slug + kiracı + sistem rolleri + `OrganizationCreated`; signup, platform ucu ve Migrator ortak kullanır), `POST /platform/organizations` (`CreateOrganizationHandler`), `PlatformAdminBootstrapper` (Migrator `create-platform-admin`), `User.GrantPlatformAdmin()`, `ISecretGenerator.NewPassword`, `AddIdentityProvisioning` (API ve Migrator ortak DI).
- **Host:** `ForwardedHeaders` (`ForwardedHeadersSetup`), `NoStoreAuthMiddleware`, `Docs:Enabled`, `AddKeyPerFile("/run/secrets")`; Worker `HeartbeatService` (konteyner `HEALTHCHECK` sinyali); `RequireConnectionString` boş değeri reddeder; Migrator `create-platform-admin` komutu ve `Dockerfile`.
- **Paketleme:** `src/Sense.Crm.{Api,Worker,Migrator}/Dockerfile`, `web/Dockerfile` + `web/nginx/`, `.dockerignore`, `deploy/` (compose, sır üreticiler, yedek/geri yükleme, TLS örnekleri, duman testi), `.github/workflows/ci.yml`.
- **Testler:** `Sense.Crm.Modules.Identity.Tests` — `PlatformApiTests`, `HostHardeningTests`.

## 16. Marketing modülü (Milestone 6C — Pazarlama)

HTTP sözleşmesinin bağlayıcı kaynağı [m6c-pazarlama.md](../plan/m6c-pazarlama.md); bu bölüm uygulanan hâli ve kararları özetler.

**Modül:** `Sense.Crm.Modules.Marketing.{Domain,Application,Contracts,Infrastructure,Api}`, şema `marketing`, tek `MarketingDbContext` (migration `InitialMarketing`: `campaigns`, `campaign_members` + outbox/inbox). `Sense.Crm.Api` (`ModuleCatalog`), `Sense.Crm.Migrator` ve `Sense.Crm.Worker`'a (outbox + `LeadConverted` tüketicisi) Sales/Activities kalıbıyla bağlandı. Sales'e yalnız `Sales.Contracts` (`IRecordLookup`, `ILeadStatusLookup`, `LeadConverted`), Identity'ye `Identity.Contracts` (`IMemberLookup`, `TenantCalendarService`, `crm.reports.read`) üzerinden bağlıdır; Sales tablolarına dokunmaz, üyelik `(memberType, memberId)` yumuşak bağıdır (FK yok). Marketing integration event **yayınlamaz**, yalnız `LeadConverted` tüketir. `Sales.Contracts`'a eklenen tek şey `ILeadStatusLookup` (`GetConvertedLeadIdsAsync`; uygulaması `Sales.Infrastructure.LeadStatusLookup`, `AddSalesContractServices()` içinde kayıtlı).

**Agregatlar** (`TenantAggregateRoot`, `IAuditLogged`, kimlikler `Guid.CreateVersion7`, EF'te `ValueGeneratedNever`; kişisel veri alanı yok → maskeleme yok):

| Agregat / tablo | Kurallar |
|---|---|
| `Campaign` / `campaigns` (yumuşak silinir) | `type` `email\|event\|webinar\|advertising\|other`; `status` `planned\|active\|completed\|cancelled` (oluştururken yalnız `planned`/`active`, sonrası yalnız `POST /campaigns/{id}/status`; `PUT` durumu değiştirmez); `startDate`/`endDate` `date`, `endDate >= startDate` (`campaign.invalid_date_range`, ayrıca `errors.endDate`); tek para birimi (`^[A-Z]{3}$`, varsayılan `TRY`); `budget/expectedRevenue/actualCost` `decimal(18,4)` ≥ 0; sahip verilmezse çağıran, verilen aktif üye olmalı (`owner.not_member`), sahibi değişmeyen kayıtta üyelik yeniden sorgulanmaz. Geçiş tablosu (`Campaign.CanTransition`): planned → active/cancelled, active → completed/cancelled, completed → active, cancelled → planned; aynı duruma geçiş no-op (204), tabloda olmayan `campaign.invalid_status_transition` (409, `args.from/to`). `completed`/`cancelled` kampanyaya üye eklenemez (`campaign.closed`); durum güncelleme, çıkarma ve `PUT` açık kalır. İndeksler: `(tenant, status)`, `(tenant, type)`, `(tenant, owner_user_id)`, `(tenant, start_date)`, `(tenant, created_at)` |
| `CampaignMember` / `campaign_members` (**fiziksel silinir**, `deleted` denetim kaydı bırakır) | `memberType` `lead\|contact`, `status` `added\|sent\|responded\|converted\|unsubscribed` (ekleme anında `added`), `addedAt`, `statusChangedAt`, `addedByUserId`. Elle ayarlanabilir: `added/sent/responded/unsubscribed` (serbest geçiş); `converted` elle ayarlanamaz (`validation`, `errors.status`), yalnız `LeadConverted` ile ve yalnız lead içindir, kilitlidir (toplu durum güncellemede `skippedCount`, tek üyeli çağrıda da 200 + `skippedCount: 1`; çıkarma yine mümkün). Benzersiz indeks `(tenant, campaign_id, member_type, member_id)`; ek indeksler `(tenant, member_type, member_id)` ve `(tenant, campaign_id, status)`. Kampanya yumuşak silinince üyelikler kalır, hiçbir uçtan görünmez (tüm sorgular kampanya filtresinden geçer) |

**Uç noktalar** (`/api/v1`, Bearer; okuma `crm.campaigns.read`, yazma `crm.campaigns.write`, rapor `crm.reports.read`): `GET/POST /campaigns`, `GET/PUT/DELETE /campaigns/{id}`, `POST /campaigns/{id}/status`, `GET /campaigns/{id}/metrics`, `GET /campaigns/{id}/members`, `POST /campaigns/{id}/members` (toplu ekleme, 200), `POST /campaigns/{id}/members/status` ve `/members/remove` (satır kimlikleriyle), `GET /campaigns/by-member?memberType&memberId` (düz dizi, en çok 200), `GET /reports/marketing/summary?from&to`. Liste filtreleri `q` (ad + açıklama, `ILIKE`, joker kaçışlı), `type`/`status` (virgülle çoklu; bilinmeyen değer `validation`), `ownerUserId`, `startFrom`/`startTo` (uçlar dahil, `startDate`'siz kampanyalar dışarıda); `sort` beyaz listesi `name, type, status, startDate, endDate, budget, actualCost, createdAt` (varsayılan `-createdAt`, boş değerler her iki yönde sonda, `createdAt` + `Id` ile kararlı). Üye listesi `memberType`, `status` (çoklu), `sort` `addedAt` (varsayılan azalan), `statusChangedAt`, `status`, `memberType`; ada göre arama/sıralama yoktur (ad Sales'te). Doğrulama yetkiden önce çalışır.

**Toplu ekleme** (`AddCampaignMembersHandler`): 1–500 kimlik (aksi `validation`, `errors.memberIds`), tekrarlar sessizce tekilleştirilir. Sıra: zaten üye olanlar (`alreadyMemberCount`) → kiracıda olmayan kayıtlar (`IRecordLookup.GetDisplayNamesAsync`, tür başına tek sorgu; 404 değil `skipped: not_found`) → dönüşmüş lead'ler (`ILeadStatusLookup`, `skipped: lead_converted`) → geri kalanı tek transaction'da eklenir; aynı çağrı tekrarlanırsa `addedCount: 0`. **Eşzamanlılık:** aynı kampanyaya eşzamanlı ekleme istekleri transaction boyunca `pg_advisory_xact_lock` ile serileştirilir (`ICampaignMemberRepository.LockCampaignMembersAsync`, kilit kampanya başına; ön kontrol ve ekleme kilit altında yapılır, commit/rollback'te kendiliğinden düşer): ikinci yazar birincinin işlenmiş satırlarını görür ve bunları `alreadyMemberCount`'a sayar → `addedCount` istekler arasında tam olarak üye sayısına toplanır; benzersiz ihlali/deadlock oluşmaz (karışık sıralı 12 eşzamanlı istekle stres testli). Savunma hattı olarak kilitsiz yolda da (`AddRangeIgnoringDuplicatesAsync`, Npgsql `23505`) EF açık transaction içindeki başarısız `SaveChanges`'ı savepoint'e geri alır, bekleyen ekleme + denetim kayıtları bırakılır, çakışan satırlar atlanıp bir kez yeniden denenir ve ihlal hata değil "zaten üye" sayılır (deterministik testli).

**Metrikler** (`Domain.Metrics.MarketingMetrics`, saf hesap; hesaplanır, saklanmaz; kampanya başına tek gruplama sorgusu): `contactedCount` = `added` dışı üyeler, `responseCount` = `responded + converted`, `responseRate` = `responseCount / contactedCount × 100`, `conversionRate` = `convertedCount / leadCount × 100` (yalnız lead), 2 ondalık (yarıdan yukarı), payda 0 → 0, `costPerLead` = `actualCost / leadCount` 2 ondalık (maliyet boşsa veya lead yoksa alan yazılmaz), `statusCounts` beş anahtarın hepsi (0 dahil). Silinmiş lead/kişiye ait üyelikler de sayılır (bilinen sınırlama).

**Rapor** (`MarketingReportStore`): kampanya aralığa `startDate ∈ [from, to]` (yoksa `createdAt`, kiracı yerel günü, UTC yarı açık sınırla) girer; aralık kuralları M3 raporlarıyla aynıdır (`TenantCalendarService.ResolveReportRangeAsync`, varsayılan son 12 ay, ters/10 yılı aşan aralık `validation`). `byStatus` (4 durum) ve `byType` (5 tür) sabit sırada ve sıfır dolduruludur; `totals` oranları toplam sayılar üzerinden hesaplanır; `topCampaigns` en çok 10 (dönüşen azalan, üye azalan, ad). Üye sayımları `campaign_members` üzerinde kampanya başına tek gruplama sorgusudur; kampanya satırlarının kendisi (id, ad, tür, durum, tutarlar) aralık içinde belleğe alınıp türe/duruma göre toplanır (pilot ölçeğinde kabul edilmiş; çok büyük kiracılar için ileride tamamen SQL toplamasına çekilebilir).

**Olay tüketimi:** `LeadConverted` → `LeadConvertedMarketingHandler` (`IIntegrationEventHandler<LeadConverted>`): olayın kiracı kapsamında `(lead, LeadId)` olan **tüm** üyelikleri (kampanya durumu önemsiz, silinmiş kampanya hariç) `converted` yapar; zaten `converted` olanlara dokunmaz (idempotent), üyeliği olmayan lead için no-op, eşleşen kişi kampanyaya eklenmez. Worker'da elle kayıtlıdır (`AddMarketingContractServices` + `MarketingDbContext` + outbox); API'de `AddModuleHandlers` tarar. Değişiklikler `CampaignMember` denetim kaydı üretir (kullanıcı boş, sistem bağlamı).

**İzinler ve roller:** `MarketingPermissions` (`crm.campaigns.read`, `crm.campaigns.write`, grup `crm`) `IModule.Permissions` ile kataloğa katılır. `SystemRoleDefinitions` **değişmedi**: Administrator kataloğun tamamını, Standard tüm `crm.*` (`crm.approvals.decide` hariç) + `org.users.read` kuralıyla yeni iki anahtarı otomatik alır; mevcut organizasyonlar API açılışında `SystemRolePermissionSynchronizer` ile senkronlanır (testli: yeni + senkronlanan eski organizasyon). Lead/kişi adları `IRecordLookup` ile gösterildiği için `crm.campaigns.read` taşıyan ama `crm.leads.read`/`crm.contacts.read` taşımayan özel rol üye adlarını görür (Activities'teki `relatedName` ile aynı, kabul edilmiş sınırlama).

**Denetim:** `Campaign` ve `CampaignMember` (`EntityType` = `Campaign` | `CampaignMember`) `GET /audit?entityType=…&entityId=…` ile `crm.campaigns.read` üzerinden okunur (`MarketingAuditEntityPermissions`); enum alanları camelCase string, alan adları camelCase. Üyeliğin fiziksel silinmesi `AuditLogInterceptor` tarafından `deleted` olarak yakalanır (testli).

**Yeni hata kodları** (metinler `SharedResource.resx` tr/en): `campaign.invalid_date_range` (400, `errors.endDate` ile), `campaign.invalid_status_transition` (409), `campaign.closed` (409); doğrulama mesajları `validation.campaign_create_status`, `validation.campaign_member_status`, `validation.campaign_type`, `validation.campaign_status`, `validation.member_status`, `validation.member_ids`, `validation.member_id`, `validation.campaign_amount`; alan adları `field.start_date`, `field.end_date`, `field.budget`, `field.expected_revenue`, `field.actual_cost`, `field.owner_user_id`, `field.member_ids`, `field.member_id`, `field.member_type`; `permission.crm.campaigns.*`. `owner.not_member` mevcut koddur.

**Testler:** `Sense.Crm.Modules.Marketing.Tests` — domain birim (geçiş tablosunun her hücresi, tarih aralığı, tutar/para birimi, üye durum kuralları, metrik formülleri/yuvarlama/payda 0, enum listesi ayrıştırma) + Testcontainers HTTP: kampanya CRUD/tam değiştirme/doğrulama/sahip kuralı/durum uçları/yumuşak silme, liste filtre/çoklu değer/sıralama (boşlar sonda)/sayfalama/joker kaçışı/tarih uçları, toplu ekleme (idempotency, tekilleştirme, `not_found`/`lead_converted` atlama, 500 gerçek üye ve 501 → 400, kapalı kampanya, **eşzamanlı ekleme**), üye listesi + toplu ad çözümü + `memberMissing`, toplu durum/çıkarma, `by-member`, **`LeadConverted` otomatik dönüşüm** (gerçek Sales outbox yoluyla, çift teslim, üyeliksiz lead, başka kiracı olayı, kilit, kişi otomatik eklenmez), **metrik doğruluğu** (elle hesaplanmış bilinen veri kümesi) ve rapor (çok kampanyalı bilinen veri, aralık uçları, `createdAt` yedeği, varsayılan aralık, top-10), **kiracı izolasyonu**, izinler (403 + doğrulama önce), sistem rolleri ve denetim. `Sense.Crm.Modules.Sales.Tests.LeadStatusLookupTests` `ILeadStatusLookup`'ı doğrular. Mimari ve kiracı izolasyonu testleri yeni modülü otomatik kapsar.

**Açık işler (Marketing):** silinmiş lead/kişi üyeliklerinin otomatik temizliği (yumuşak bağ; `memberMissing` olarak kalır, metrik/rapor yine sayar), e-posta gönderimi/şablonları, segmentasyon, CSV ile üye içe aktarma, kampanya bazlı ROI ve kazanılan fırsat atfı (fırsat → kampanya bağı), lead'e kampanya kolonu/otomatik `source = campaign`, dönüşen lead'in kişisinin otomatik üyeliği, üye listesinde ada göre arama (`IRecordLookup` arama uzantısıyla), raporlar çok para birimli tutarı ayırmadan toplar (M3 sınırlaması), rapor kampanya satırlarını bellekte toplar (büyük kiracılar için tamamen SQL toplaması).
## 17. Commerce modülü (Milestone 6A — ürünler, teklifler, satış siparişleri)

HTTP sözleşmesinin bağlayıcı kaynağı [m6a-ticaret.md](../plan/m6a-ticaret.md); bu bölüm uygulanan hâli ve planın sessiz kaldığı yerlerdeki kararları özetler.

**Modül:** `Sense.Crm.Modules.Commerce.{Domain,Application,Contracts,Infrastructure,Api}`, şema `commerce`, tek `CommerceDbContext` (migration `InitialCommerce`). `Sense.Crm.Api` (`ModuleCatalog`), `Sense.Crm.Migrator` ve `Sense.Crm.Worker` (outbox: `QuoteAccepted`, `SalesOrderCreated`; M6A'da tüketici yok) Sales/Activities ile aynı kalıpla bağlandı. Ürün, teklif, sipariş ve numara sayacı aynı işlem sınırında yazıldığı için **tek modüldedir**. Diğer modüllere yalnız `Sales.Contracts` (`IRecordLookup`, yeni `IRecordRelationLookup`) ve `Identity.Contracts` (`IMemberLookup`, `TenantCalendarService`, `IAuditEntityPermissions`) üzerinden bağlıdır; Sales'in tablolarına doğrudan erişim yoktur ve fırsat aşaması otomatik değişmez.

**Contracts eklemeleri:** `Sales.Contracts`: `ContactLink`, `DealLink`, `IRecordRelationLookup` (kiracı + yumuşak silme filtresi altında; başka kiracı/silinmiş → `null`; `Sales.Infrastructure.RecordRelationLookup`, `AddSalesContractServices()` içinde kayıtlı). `Commerce.Contracts`: `CommercePermissions` (`crm.products|quotes|orders.read|write`, grup `crm`), `CommerceAuditEntities` (`Product`, `Quote`, `SalesOrder` → ilgili `*.read`), `QuoteAccepted`, `SalesOrderCreated`. İzin anahtarları `GET /permissions` kataloğuna girer; `SystemRoleDefinitions` **değişmedi** (Administrator kataloğun tümü, Standard tüm `crm.*` — `crm.approvals.decide` hariç — prefix kuralıyla yeni anahtarları otomatik alır; mevcut organizasyonlar API açılışında `SystemRolePermissionSynchronizer` ile senkronlanır, testli).

### Agregatlar ve kalıcılık

| Varlık | Not |
|---|---|
| `Product` | `TenantAggregateRoot`, `IAuditLogged`, yumuşak silinen. `code` kiracıda benzersiz, büyük/küçük harf duyarsız: `code_normalized` (`ToUpperInvariant`) + `(tenant_id, code_normalized) WHERE is_deleted = false AND code_normalized IS NOT NULL` benzersiz indeksi (`ux_products_tenant_code`); ihlal → `product.code_taken` 409. Boş kod null saklanır |
| `Quote` / `SalesOrder` | Ortak taban `SalesDocument` (numara, konu, firma/kişi/fırsat, sahip, para birimi, şartlar/notlar, sunucu toplamları). `IAuditLogged`, yumuşak silinen; Npgsql **`xmin` eşzamanlılık belirteci** (`Version`, migration kolon üretmez). Durum geçişleri ve `DocumentTotals` agregatın içindedir (Result deseni) |
| `QuoteLine` / `SalesOrderLine` | Başlığın çocuk tabloları (`ITenantEntity`, `(tenant_id, …)` indeksli, `IAuditLogged` değil). Kalemler ürün değerlerinin **anlık görüntüsüdür** (`product_id` yumuşak bağ) |
| `DocumentCounter` | `commerce.document_counters (tenant_id, kind, year, last_value)`, PK `(tenant_id, kind, year)`; yalnız ham SQL ile yazılır |

**Toplamlar (`DocumentTotals`, saf domain):** `MidpointRounding.AwayFromZero`, 2 ondalık, **kalem bazında** yuvarlama; belge toplamları yuvarlanmış kalem değerlerinin toplamıdır (`grandTotal = Σ lineTotal`). Planın dört referans vektörü ve belge {1,3} toplamı (85.22 / 6.00 / 15.34 / 94.56) birim testlidir; web `computeTotals` aynı tabloyu kullanır. Her yazmada (oluştur/güncelle/dönüştür) baştan hesaplanır; gövdedeki hesaplanan alanlar yok sayılır. Saklama: adet/birim fiyat `decimal(18,4)`, yüzdeler `decimal(5,2)`, tutarlar `decimal(18,2)`. **Karar (plan sessiz):** belge toplamı `decimal(18,2)` sınırını (9.999.999.999.999.999,99) aşarsa kayıt reddedilir: `commerce.total_too_large` 400 (100 uç değerli kalem taşabilirdi); kontrol numara ayrılmadan önce yapılır.

**Durum makineleri:** Teklif `draft → sent → accepted | rejected`, `revert` (`sent`/`rejected` → `draft`), `extend` (`sent`); `accepted` uçtur; düzenleme/silme yalnız `draft`. `expired` **saklanmaz, türetilir**: `status = sent` ve `validUntil` dolu ve `validUntil < bugün (kiracı saat dilimi)`; tek tanım `QuoteStatusExpression` (liste filtresi ve rapor EF ifadesi olarak, DTO eşlemesi bellek içi aynı kural; eşdeğerlik birim testle korunur). Sipariş `draft → confirmed → fulfilled`, `draft | confirmed → cancelled`; `fulfilled`/`cancelled` uçtur. Tanımsız geçiş `…invalid_transition` 409 (`args.from`/`args.to` camelCase durum adları); ikinci `accept` idempotent değil 409.

### Numaralandırma (kiracı + tür + yıl, eşzamanlılığa dayanıklı)
Numara `Q-{yıl}-{sıra:D4}` / `SO-{yıl}-{sıra:D4}` (9999'u aşarsa doğal uzar); **yıl oluşturma anında kiracının saat diliminde** hesaplanır (Istanbul'da 2026-12-31T21:30Z → `…-2027-0001`). Ayırma, belge INSERT'iyle **aynı transaction'da** tek atomik ifadeyle yapılır (`INSERT … ON CONFLICT (tenant_id, kind, year) DO UPDATE SET last_value = last_value + 1 RETURNING last_value`, `TenantId` her zaman `ITenantContext`'ten parametre); satır kilidi transaction sonuna dek tutulur, eşzamanlı oluşturmalar sıraya girer. Yedek güvence: `(tenant_id, number)` benzersiz indeks (silinmişler dahil; `ux_quotes_tenant_number`, `ux_sales_orders_tenant_number`). Silinmiş belgenin numarası yeniden kullanılmaz.

**`ICommerceTransaction` (savepoint):** komutların yazma bölümü UnitOfWork transaction'ı içinde bir **savepoint** açar; iş bloğu hata dönerse veya kayıt çakışırsa savepoint'e dönülür ve hata iş koduna çevrilir. Sebep: UnitOfWork başarısız `Result`'ta da transaction'ı commit eder; savepoint olmadan numara sayacı artışı (ve EF'in kısmen yazdığı satırlar) kalıcı olur, "numara boşluksuz / hiçbir şey kalıcı olmaz" güvencesi bozulurdu. Eşleme: `DbUpdateConcurrencyException` (xmin) → `commerce.concurrent_update` 409; `ux_products_tenant_code` → `product.code_taken`; `ux_sales_orders_tenant_quote` → `quote.already_converted` (numara indeksleri → `commerce.concurrent_update`). Yumuşak silme belgede `IsDeleted` işaretlenerek yapılır (`EntityState.Deleted` kalemleri cascade ile fiziksel silerdi); yalnız kalemi değişen belgede başlığın `xmin`'i `ModifiedDate` işaretiyle ilerletilir (kalem yarışı da yakalanır).

### Teklif → sipariş dönüşümü (`POST /quotes/{id}/convert`, tek transaction)
İzin `crm.quotes.read` + `crm.orders.write`; yanıt 201 + `Location: /api/v1/orders/{id}` + sipariş detayı (handler'dan döner; `crm.orders.read` gerekmez — aynı gerekçeyle `POST` uçları da detay gövdesini handler'dan döner, yazma izni yeterlidir). Akış: teklif `accepted` olmalı (`quote.not_accepted`), bağlı silinmemiş sipariş yok (`quote.already_converted`), firma/kişi/fırsat yeniden doğrulanır (`commerce.related_not_found`); sayaç, sipariş (`draft`, `orderDate` = bugün), yeni kimlikli kalemler (`DocumentTotals` ile yeniden hesaplanır → tutarlar birebir), `SalesOrderCreated` (`source: quote`) outbox olayı **aynı savepoint/transaction**'dadır. Çift dönüşüm engeli: `sales_orders (tenant_id, quote_id) WHERE quote_id IS NOT NULL AND is_deleted = false` benzersiz indeksi; iki eşzamanlı dönüşümde ikincisi indeks ihlaliyle düşer, savepoint geri alınır (sayaç dahil), `quote.already_converted` 409 döner — asla iki sipariş, asla numara boşluğu (5 turluk yarış testi). Taslak siparişi silmek teklifi yeniden dönüştürülebilir yapar; iptal edilmiş sipariş silinemediğinden teklifi yeniden dönüştürülemez kılar. `QuoteAccepted` olayı kabul ile aynı transaction'da yazılır; xmin çakışması sayesinde çift `accept` yarışında yalnız bir olay çıkar (testli).

### Uçlar ve kurallar (özet; ayrıntı plan belgesinde)
- `GET/POST /products`, `GET/PUT/DELETE /products/{id}` (`crm.products.*`); `q` ad + kod + açıklama (`ILIKE`, kaçışlı parametre), `isActive`, `currency`; `sort`: `name` (varsayılan), `code` (kodsuzlar her yönde sonda), `unitPrice`, `createdAt`, `updatedAt`.
- `GET/POST /quotes`, `GET/PUT/DELETE /quotes/{id}`, `POST /quotes/{id}/{send|accept|reject|revert|extend|convert}` (`crm.quotes.*`); liste filtreleri `status` (etkin durum), `accountId`, `contactId`, `dealId`, `ownerUserId`, `validFrom/validTo`, `converted`; `sort`: `number`, `subject`, `grandTotal`, `validUntil` (boş her iki yönde de sonda), `createdAt` (varsayılan `-createdAt`).
- `GET/POST /orders`, `GET/PUT/DELETE /orders/{id}`, `POST /orders/{id}/{confirm|fulfill|cancel}` (`crm.orders.*`); `orderFrom/orderTo`, `quoteId` filtreleri.
- `GET /reports/commerce/summary?from&to` (`crm.reports.read`): teklifler `createdAt` (kiracı yerel günü), siparişler `orderDate` aralığına göre; `byStatus` her zaman tüm durumları sabit sırayla döner; teklif durumları etkin durumdur (`expired` bugünün kiracı tarihine göre); `orders.totalCount/totalAmount` iptaller hariç; `conversionRate` = `accepted` / taslak olmayan teklifler (4 ondalık, payda 0 → alan yok); `currencies` sıralı tekil liste.
- Kalem doğrulaması `errors["lines[i].alan"]` anahtarlarıyla (adet > 0 ≤ 1.000.000 ≤ 4 ondalık, birim fiyat 0–1e9 ≤ 4 ondalık, iskonto/KDV 0–100 ≤ 2 ondalık, en çok 100 kalem); ürün doğrulaması handler'da `ValidationException` ile (`lines[i].productId`: kiracıda yok, belgede daha önce olmayan pasif ürün, para birimi uyuşmazlığı). Geçerlilik tarihi hataları `errors.validUntil`. Sahip kuralı `owner.not_member` (Sales/Activities ile aynı). Doğrulama yetkiden önce çalışır.
- Denetim: `Product`, `Quote`, `SalesOrder` `IAuditLogged` (enum'lar camelCase string; kişisel veri yok → maskelenen alan yok); kalem değişikliği başlığın `subtotal/discountTotal/taxTotal/grandTotal` farkıyla görünür; `GET /audit?entityType=Product|Quote|SalesOrder&entityId=` ilgili `*.read` ile.

**Yeni hata kodları** (metinler `SharedResource.resx` tr/en): `commerce.related_not_found` (404), `commerce.contact_account_mismatch` (400), `commerce.deal_account_mismatch` (400), `commerce.concurrent_update` (409), `commerce.total_too_large` (400, plan dışı), `product.code_taken` (409), `quote.not_editable` (409), `quote.invalid_transition` (409), `quote.expired` (409), `quote.no_lines` (422), `quote.not_accepted` (409), `quote.already_converted` (409), `order.not_editable` (409), `order.invalid_transition` (409), `order.no_lines` (422); mevcut: `validation`, `not_found`, `forbidden`, `owner.not_member`.

**Testler:** `Sense.Crm.Modules.Commerce.Tests` — birim (`DocumentTotals` referans vektörleri + özellik tabanlı değişmez, teklif/sipariş durum makineleri, `expired` türetimi + ifade/bellek eşdeğerliği + saat dilimi sınırı, ürün kuralları, numara biçimi) ve Testcontainers HTTP testleri: ürün CRUD/filtre/sıralama/joker kaçışı/`code_taken`; teklif CRUD, `lines[i].*` doğrulama anahtarları, sahip/bağlı kayıt kuralları, ürün anlık görüntüsü/pasif/para birimi, sunucu toplamı, filtre/sıralama, durum makinesi, `expired` (kiracı saat dilimi dahil), denetim; **numaralandırma** (20 paralel `POST /quotes`, kiracı/tür bağımsızlığı, yıl dönümü sahte saatle, hata enjeksiyonunda sayaç geri dönüşü); **dönüşüm** (birebir tutarlar, outbox olayları, çift dönüşüm, eşzamanlı dönüşüm ve kabul yarışı, kalem yazımını patlatan hata enjeksiyonunda hiçbir şeyin kalıcı olmaması, taslak sipariş silince yeniden dönüştürme, iptal edilmiş sipariş, izin birleşimi); sipariş; rapor doğruluğu (3/9 = 0.3333, iptal hariç toplam, aralık uçları kiracı saatinde, varsayılan aralık, `currencies`); **kiracı izolasyonu** (ürün/teklif/sipariş kimlikleri, geçişler, dönüşüm, kalemde yabancı `productId`, yabancı firma/kişi/fırsat, liste/rapor/denetim sızıntısı, sayaç); izin matrisi (her uç için `*.read`/`*.write`), Standard rolün altı anahtarı taşıması ve eski kiracıda senkronu. Mimari ve kiracı izolasyonu testleri yeni modülü otomatik kapsar.

## 18. Milestone 6A'da değişenler (backend)
- Yeni modül `Commerce` (şema `commerce`, migration `InitialCommerce`: `products`, `quotes`, `quote_lines`, `sales_orders`, `sales_order_lines`, `document_counters` + outbox/inbox; indeksler kiracı ön ekli; benzersiz: ürün kodu (filtreli), teklif/sipariş numarası, teklif başına silinmemiş tek sipariş); `Sense.Crm.Api` (`ModuleCatalog`), `Sense.Crm.Migrator`, `Sense.Crm.Worker`'a bağlandı; yeni test projesi `Sense.Crm.Modules.Commerce.Tests`; `CrmApiFactory` Respawn şemalarına `commerce` eklendi.
- `Sales`: `IRecordRelationLookup` + `RecordRelationLookup` (mevcut davranış değişmedi).
- `SharedResource{,.en}.resx`: Commerce hata/doğrulama/alan/izin anahtarları (tr + en).
- Mevcut testlerde izin kataloğu için tam sayı beklentisi (18) kaldırıldı (anahtarlar `Contain` ile doğrulanır; katalog boyutu modül eklendikçe değişir).
- **Açık işler:** PDF/yazdırma, e-posta ile gönderme, kur çevrimi, stok/sevkiyat/faturalama, fiyat listeleri, belge düzeyi iskonto, teklif revizyon geçmişi, teklif kabulünde fırsat aşamasının otomatik değişmesi, `QuoteAccepted`/`SalesOrderCreated` tüketicileri (workflow/M7) — plan kapsamı dışı; çok Worker/yüksek yük altında numara sayacı satırı kiracı+tür+yıl başına serileşir (bilinçli; belge başına tek satır kilidi).
## 19. Service modülü (Milestone 6B — servis/destek: talepler, yorumlar, SLA)

HTTP sözleşmesinin bağlayıcı kaynağı [m6b-servis.md](../plan/m6b-servis.md); bu bölüm uygulanan hâli ve mimari kararları özetler.

**Modül:** `Sense.Crm.Modules.Service.{Domain,Application,Contracts,Infrastructure,Api}`, şema `service`, tek `ServiceDbContext` (migration `InitialService`: `dotnet dotnet-ef migrations add InitialService --project src/Modules/Service/Sense.Crm.Modules.Service.Infrastructure --startup-project src/Sense.Crm.Migrator --context ServiceDbContext -o Persistence/Migrations`). Sales'e yalnız `Sales.Contracts` (`IRecordLookup`, yeni `IContactAccountLookup`), Identity'ye `Identity.Contracts` (`IMemberLookup`, `ITenantDirectory`/`TenantCalendarService`, `IAuditEntityPermissions`, `OrganizationCreated`, `CrmPermissions.ReportsRead`) üzerinden bağlıdır (mimari testler yeşil). Rapor uçları aynı modüldedir (ayrı Reporting modülü yok).

**Varlıklar** (hepsi kiracıya ait; kimlikler `Guid.CreateVersion7`, EF'te `ValueGeneratedNever`):

| Varlık / tablo | Kurallar |
|---|---|
| `Case` / `cases` (`IAuditLogged`, yumuşak silinir) | Numara `C-{yıl}-{sıra:D4}`; durum `new\|open\|pending\|resolved\|closed`, öncelik `low\|normal\|high\|urgent`, kanal `email\|phone\|web\|other`; firma/kişi yumuşak bağ (`accountId?`, `contactId?`); atanan aktif üye (`owner.not_member`), verilmezse atanmamış; `resolutionNote`, `firstResponseAt/resolvedAt/closedAt`, `reopenCount`; SLA anlık görüntüsü `firstResponseDueAt`, `dueAt`, `slaAnchorAt`, `firstResponseWarnAt`, `resolutionWarnAt`. Eşzamanlılık PostgreSQL `xmin` belirteciyle (`general.concurrency_conflict`, 409) |
| `CaseComment` / `case_comments` (`IAuditLogged`, değişmez) | `visibility` `public\|internal`, `body` 1–8000 (denetimde `***` maskelenir), yazar; düzenleme/silme yok |
| `CaseEvent` / `case_events` | Zaman çizelgesi olayları (`created\|statusChanged\|priorityChanged\|assigned`), yalnız ekleme; **denetim dışı** (kendisi değişmez olay günlüğü; Lead onaylı bilinçli istisna) |
| `CaseCounter` / `case_counters` | `(TenantId, Year)` bileşik anahtar, `lastValue`; **denetimsiz teknik tablo** (Lead onaylı istisna), denetim kolonu da yok |
| `SlaPolicy` / `sla_policies` (`IAuditLogged`) | Öncelik başına bir satır (kiracıda tam 4), `firstResponseMinutes`, `resolutionMinutes`; `(TenantId, Priority)` benzersiz |

İndeksler hep `TenantId` ile başlar: `cases` `(tenant, number)` benzersiz, `(tenant, status, created_at desc)`, `(tenant, assigned_user_id, status)`, `(tenant, account_id)`, `(tenant, contact_id)`, `(tenant, due_at)`; `case_comments`/`case_events` `(tenant, case_id, created_at)`.

**Numaralama:** yıl, oluşturma anının **kiracı saat dilimindeki** takvim yılıdır (`TenantCalendarService`). `ICaseNumberGenerator` talep INSERT'iyle **aynı transaction'da**, doğrulamalardan sonra en son adım olarak tek ham SQL çalıştırır: `INSERT INTO service.case_counters … ON CONFLICT (tenant_id, year) DO UPDATE SET last_value = case_counters.last_value + 1 RETURNING last_value` (`TenantId` `ITenantContext`'ten). Satır kilidi eşzamanlı oluşturmaları commit'e kadar sıraya dizer → numara benzersiz ve boşluksuz; transaction geri alınırsa sayaç da geri alınır (numara yanmaz); silinen talebin numarası yeniden kullanılmaz. 9999'u aşarsa basamak artar (`C-2026-10000`).

**Durum makinesi** (`CaseRules.IsTransitionAllowed`, domain'de saf): new → {open, pending, resolved, closed}; open → {pending, resolved, closed}; pending → {open, resolved, closed}; resolved → {open, closed}; closed → {open}. Tablo dışı geçiş `case.invalid_transition` (409, `from`/`to` argümanları); aynı duruma geçiş idempotent 204 (olay/`CaseResolved` yok). Çözme ve çözülmeden kapatmada `resolutionNote` zorunlu (`case.resolution_required`, 400); çözüm anı `resolvedAt`'tir ve `firstResponseAt` boşsa ilk yanıt sayılır. Yeniden açma: `resolved → open` sınırsız, `closed → open` `closedAt`'tan itibaren 14 gün (`Service:ReopenWindowDays` ile ayarlanır, `case.reopen_window_expired`); `reopenCount++`, `resolvedAt/closedAt/resolutionNote` temizlenir, `slaAnchorAt = şimdi` ve `dueAt` yeniden hesaplanır. `PUT`, `priority`, `assign` yalnız aktif talepte (`case.not_active`), yorum kapalı talepte `case.closed` (409), çözülmüşte serbesttir. Her geçiş/öncelik/atama bir `CaseEvent` yazar.

**SLA** (duvar saati dakikası; iş saati/tatil ve `pending`'de duraklatma kapsam dışı — Lead onaylı): hedefler oluşturmada, öncelik değişiminde (**özgün başlangıçtan**) ve yeniden açmada hesaplanıp **saklanır** (politika sonradan değişirse mevcut talepler değişmez). Uyarı eşiği süresinin %80'i tam tamsayı saniyeyle (`dakika × 48 sn`). Okuma anında (`TimeProvider`; yanıt, `slaState` süzgeci ve özet aynı tanım): `firstResponseBreached = (firstResponseAt ?? now) > firstResponseDueAt`, `resolutionBreached = (resolvedAt ?? now) > dueAt`; `slaState` = `breached` / (aktif ve eşik aşıldıysa) `atRisk` / `ok`. Hedef anın kendisi ihlal **değil**, `atRisk`'tir; bir tik sonrası ihlaldir. Saf hesap `CaseSlaCalculator`; SQL karşılığı `CaseSlaQuery` ifadeleridir (testler ikisinin aynı kümeyi verdiğini doğrular). Varsayılan politikalar (ilk yanıt / çözüm dk): urgent 60/240 · high 240/1440 · normal 480/4320 · low 1440/10080; doğrulama `1 ≤ ilk yanıt ≤ çözüm ≤ 525600`.

**Yorum ve ilk yanıt:** `visibility` açıkça verilir (varsayılan yok). `firstResponseAt` boşken herkese açık yorum ilk yanıttır ve `new` talebi `open` yapar (olay aktörü yorumu yazan); dahili yorum sayılmaz. Yorum işleyicisi xmin çakışmasında (`ICaseRepository.TrySaveAsync`: komut transaction'ının içinde, EF'in otomatik savepoint'ine geri alma) tek sefer yeniden yükleyip dener; yine çakışırsa 409. Test iki eşzamanlı ilk yorumun ikisinin de kaydolduğunu (yorum/denetim satırı çoğalmadan) doğrular.

**Uç noktalar** (`/api/v1`, Bearer; izinler `crm.cases.read` / `crm.cases.write`, yazma okumayı içermez; doğrulama yetkiden önce):

| Yol | Notlar |
|---|---|
| `GET /cases` | Filtre: `q` (numara + konu, `ILIKE`, joker kaçışlı), `status`/`priority` (virgülle çoklu), `channel`, `assignedUserId`, `unassigned=true`, `accountId`, `contactId`, `slaState`; `sort`: `number, subject, status, priority, dueAt, createdAt, updatedAt` (varsayılan `-createdAt`; durum/öncelik enum sırası); bilinmeyen enum → `validation` |
| `GET /cases/summary` | `{ openCount, overdueCount, mineCount, unassignedCount }` (aktif talepler; `overdue` = aktif ve `slaState=breached`) |
| `GET /cases/{id}`, `POST /cases` (201 + `Location`), `PUT /cases/{id}` (204, tam değiştirme), `DELETE /cases/{id}` (204, yumuşak) | Firma/kişi yalnız değiştiğinde doğrulanır; `contactId` verilip `accountId` verilmezse firma kişiden türetilir; farklı firma `case.contact_account_mismatch` |
| `POST /cases/{id}/status`, `/priority`, `/assign` | 204; `assignedUserId: null` atamayı kaldırır |
| `POST /cases/{id}/comments` | 201 `{ id, caseId, visibility, body, authorUserId, authorName, createdAt }` |
| `GET /cases/{id}/timeline?page&pageSize` | Yorumlar + olaylar tek `UNION ALL` projeksiyonu, `createdAt desc, id desc` |
| `GET/PUT /service/sla-policies` | `org.settings.manage`; GET her zaman 4 satır (`low, normal, high, urgent`, eksikse tembel tohumlanır); PUT tam 4 öncelik (`errors["policies"]`, `errors["policies[2].firstResponseMinutes"]`) |
| `GET /reports/service/summary`, `/by-assignee` | `crm.reports.read`; kohort `createdAt ∈ [from, to]` (kiracı saat diliminde takvim günü, varsayılan son 12 ay), silinmemiş; `byStatus`/`byPriority` sıfır dolgulu; ortalamalar dakika (1 ondalık), örneklem yoksa alan yok; ihlal rapor anındaki `isSlaBreached` |
| `GET /audit?entityType=Case\|CaseComment&entityId=` | `crm.cases.read` (`ServiceAuditEntityPermissions`); `SlaPolicy` yalnız `org.audit.read`; `CaseComment.body` `***` |

**Yeni hata kodları** (metinler `SharedResource.resx` tr/en): `case.account_not_found` (404), `case.contact_not_found` (404), `case.contact_account_mismatch` (400), `case.invalid_transition` (409), `case.resolution_required` (400), `case.reopen_window_expired` (409), `case.not_active` (409), `case.closed` (409); mevcut `owner.not_member`, `general.concurrency_conflict`; doğrulama mesajları `validation.sla_policies`, `validation.sla_minutes`, `validation.case_filter`, `validation.case_unassigned`; `permission.crm.cases.*` çevirileri.

**Olay:** `CaseResolved` (`Service.Contracts`, K10): `resolvedAt` yazan her geçişte (`→ resolved` ve çözülmeden `→ closed`) `IIntegrationEventOutbox.Enqueue` ile **aynı `SaveChanges`/transaction**'da yazılır; yeniden açılıp tekrar çözülen talep yeni olay üretir; aynı-durum ve hatalı geçiş olay üretmez. Yük: `caseId, caseNumber, accountId?, contactId?, priority, assignedUserId?, resolvedAt, resolutionMinutes` (tam dakika, aşağı yuvarlanır), `slaBreached` (çözüm anındaki değerlendirme). Bugün tüketicisi yoktur (bildirim/workflow için hazır); Worker `OutboxPollingService<ServiceDbContext>` çalıştırır.

**SLA politikası tohumlama** (Sales `DefaultPipelineSeeder` kalıbı): `IDefaultSlaPolicySeeder.EnsureAsync(tenantId)` — organizasyon başına advisory lock + kilit sonrası yeniden kontrol, **eksik öncelikler** varsayılanla eklenir (idempotent). Üç yol: (1) Worker'da `OrganizationCreatedSlaHandler : IIntegrationEventHandler<OrganizationCreated>` (Sales'in tüketicisiyle birlikte), (2) API açılışında `DefaultSlaPolicySyncHostedService` (mevcut organizasyonlar, `ITenantDirectory`), (3) tembel güvence: `SlaPolicyProvider` talep oluşturma/öncelik değiştirme/yeniden açma ve `GET /service/sla-policies` başında politika eksikse tohumlar.

**İzinler:** `ServicePermissions` (`crm.cases.read`, `crm.cases.write`, grup `crm`) `ServiceModule.Permissions` ile kataloğa katılır; `SystemRoleDefinitions` değişmedi (Administrator tüm katalog, Standard tüm `crm.*` → iki anahtarı otomatik alır; mevcut organizasyonlar API açılışında `SystemRolePermissionSynchronizer` ile senkronlanır). Testler izin sayısını sabitlemez (`Contain`).

**Testler:** `Sense.Crm.Modules.Service.Tests` — domain birim testleri (5×5 durum geçiş tablosu, çözüm notu, yeniden açma ve 14. gün sınırı, öncelik değişiminde SLA yeniden hesabı, ilk yanıt kuralı, SLA politikası doğrulaması) + Testcontainers HTTP testleri (sahte saatli host `ClockedHost`): numaralama (ardışık, 20 eşzamanlı POST boşluksuz, kiracı saat diliminde yıl sınırı, geri almada numara yanmaz, 10000. numara), tablo hücreleri HTTP üzerinden, SLA sınırları (`warnAt` tam eşik/bir tik önce, `now == dueAt` ihlal değil, +1 ms ihlal), politika anlık görüntüsü, süzgeç/özet/yanıt tutarlılığı, CRUD/firma-kişi kuralları, liste süzgeç/sıralama/sayfalama, zaman çizelgesi, eşzamanlı ilk yorumlar, izinler, kiracı izolasyonu (talep/firma/kişi/üye/SLA/sayaç/rapor/denetim), raporlar (bilinen veri kümesi, UTC+14 kiracı, varsayılan aralık), denetim + maskeleme + kayıt bazlı okuma, `CaseResolved` outbox, tohumlama yolları. `Sense.Crm.Tests.Architecture` ve `Sense.Crm.Tests.TenantIsolation` yeni modülü ve beş varlığı otomatik kapsar.

**Açık işler:** e-postadan talep açma, iş saatleri/tatil takvimi, `pending`'de SLA duraklatma, müşteri portalı, yükseltme (escalation) ve SLA bildirimleri, dosya ekleri, yorum düzenleme/silme, otomatik atama, talep-workflow'ları (`CaseResolved` hazır), çözülmüş talebin otomatik kapanması, firma/müşteri bazlı SLA (kapsam dışı, bkz. plan); talep silmede yumuşak bağ temizliği yok (bağlı firma/kişi silinirse ad boş döner); `POST /cases` yalnız `crm.cases.write` olan kullanıcıda kaydı oluşturur ama yanıtta detayı okuyamadığı için 403 döner (M2/M3 ile aynı bilinen sınırlama: yazma okumayı içermez); `createdAt` interceptor'ın `SaveChanges` anıdır, SLA hedefleri komut işleyicisinin `TimeProvider` anından hesaplanır (aynı saat, milisaniye farkı olabilir).

## 20. Milestone 6B'de değişenler (backend)
- Yeni modül `Service` (şema `service`, migration `InitialService`: `cases`, `case_comments`, `case_events`, `case_counters`, `sla_policies`); `Sense.Crm.Api` (`ModuleCatalog`), `Sense.Crm.Migrator`, `Sense.Crm.Worker`'a bağlandı; yeni test projesi `Sense.Crm.Modules.Service.Tests`; `CrmApiFactory` Respawn şemalarına `service` eklendi.
- `Sales.Contracts`: `IContactAccountLookup` + `ContactAccountLink` (uygulama `Sales.Infrastructure.ContactAccountLookup`, `AddSalesContractServices()` içinde); `IRecordLookup` değişmedi.
- `Identity.Contracts`/`Shared`/`Kernel`: ekleme yok. Yeni izinler `crm.cases.read/write` (`GET /permissions` sayısı modüllere göre büyür; iki mevcut test sayı yerine `Contain` doğrular).
- `SharedResource` tr/en: `case.*` hata anahtarları, `validation.sla_*`/`validation.case_*`, yeni `field.*` adları, `permission.crm.cases.*`.
## 21. Güvenlik sertleştirme (C-SEC)

Bulgu → durum tablosu: [../security/hardening-report.md](../security/hardening-report.md). Bu bölüm mimari kararları özetler.

### 16.1 Conductor görev girdisine güven (H1)
- `WorkflowTaskRunner.ExecuteAsync(taskType, workflowInstanceId, inputData)` — Worker `ConductorTask.WorkflowInstanceId`'yi iletir. Görev çalışmadan **önce**: (1) `(tenantId, executionId)` için yürütme satırı vardır (kiracı filtresi altında: başka kiracının satırı bulunmaz), (2) yürütme `running`, (3) kayıtlı `engine_workflow_id` = görevin geldiği workflow örneği, (4) görev türü yürütmenin türüne uyar (`IWorkflowTaskWorker.ExecutionKind`). Aksi hâlde yan etkisiz **terminal** hata `untrusted_task`; `engine_workflow_id` henüz yazılmamışsa (başlatma–kayıt yarışı) geçici `engine_id_pending` (Conductor yeniden dener).
- İşleyiciler ham `TaskInput` yerine `TrustedTask` alır: konu (lead/fırsat kimliği, ad, tutar, para birimi) yürütmenin saklı anlık görüntüsünden, rol kimlikleri/`followUpHours` yürütmenin `ruleId`'sindeki **kayıtlı kuraldan** (`WorkflowInputs.Build` artık rol/parametre göndermez; kural silinmişse `rule_not_found`). Follow-up görevinin `ownerUserId`'si önceki görev çıktısı olduğundan kuralın rolünün aktif üyesi olduğu yeniden doğrulanır.
- **Karar** `crm_record_decision`'da HUMAN görev çıktısından değil `approvals` satırlarından türetilir (karar/yorum/onaylayıcı); karar verilmiş satır yoksa not yazılmaz (`decision_not_found`, geçici: karar yazımı henüz commit edilmemiş olabilir → Conductor yeniden dener, kalıcı olarak yoksa yürütme başarısız). Tanım JSON'larından (`crm_lead_assignment.v1.json`, `crm_deal_approval.v1.json`) rol/karar/onaylayıcı girdileri çıkarıldı (v1 yerinde güncellendi: yayın öncesi olduğundan yeni sürüm açılmadı; uçuştaki eski workflow'lar fazla girdiyi yok sayar).
- **L3:** `crm_create_approvals` fırsat sahibini (`IRecordLookup.GetOwnerUserIdAsync`, Sales.Contracts) onaylayıcılardan çıkarır; kimse kalmazsa (tek üye sahibin kendisiyse dahil) `no_approver`.
- Test altyapısı: `FakeWorkflowEngine` artık görevleri başlatan işlem **commit edildikten sonra** çalıştırır (`DrainAsync`; her HTTP isteğinden sonra `FakeEngineDrainStartupFilter`, outbox boşaltmada ve `SyncExecutionsAsync`'te çağrılır) — gerçek motorun ayrı işlemde çalışma sırası.

### 16.2 Hesap modeli (H4)
- `User.MustChangePassword` + `Membership.Status` (`Active|Pending`) + `RefreshToken.FamilyExpiresAt` (migration `SecurityHardening`; mevcut üyelikler `Active`, mevcut token'ların aile ömrü kendi `expires_at`'ı).
- **Zorunlu parola değişimi** stateless: access token'a `pwd_change=true` claim'i konur; `PasswordChangeRequiredMiddleware` (`UseAuthentication` → `UseRateLimiter` → `UseRequestContext` → **bu** → `UseAuthorization`) bayraklı token ile `[AllowWhenPasswordChangeRequired]` (veya `[AllowAnonymous]`) olmayan her uca 403 `auth.password_change_required` verir. İzinli uçlar: `GET /me`, `POST /me/password`, `GET/POST /me/invitations*` (+ anonim `auth/*`). Parola değişince yeni token bayraksızdır; bayraklı eski access token'lar ≤ 15 dk içinde ölür.
- **Bekleyen üyelik** her yerde aktif değildir (`IsActive = false`): izin çözümü, `/me.organizations`, `switch-organization`, `IMemberLookup`, `IRoleMemberLookup`. Davet okuma/kabul/red satırı kiracı filtresini bilinçli aşar (`IMembershipRepository.ListPendingOfUserAcrossTenantsAsync`/`GetPendingInvitationAsync`, §6'daki filtre atlama listesine 4. ve 5. yer) ve **her zaman** (davet kimliği + hesap sahibi + bekliyor) ile daraltılır; yazma davetin organizasyonunun kiracı kapsamında yapılır.
- Yönetici hiçbir yerde parola seçmez: üye ekleme (sunucu üretimi), `POST /platform/organizations` (mevcut hesap → bekleyen Administrator daveti).
- `DelegationGuard` (M7): `CanGrantAsync(izinler)` = çağıranın etkin izinlerinin alt kümesi ya da `Administrator` sistem rolü; rol oluşturma/güncelleme, üye ekleme, üyenin rolünü değiştirme/yeniden etkinleştirme kullanır. Kendi üyeliğini değiştirme `member.cannot_modify_self` (`member.last_admin` denetiminden önce).

### 16.3 Giriş ve oturum (M3, M5, L5)
- `LoginHandler`: (1) e-posta kovası (`ILoginThrottle.TryAcquireEmailBucket`, `RateLimiting:LoginEmail`, varsayılan dakikada 20) + IP+hesap engeli → 429; (2) kullanıcı yoksa `IPasswordHasher.VerifyDummy` (aynı maliyet); (3) parola **her zaman önce** doğrulanır; yanlışsa genel `auth.invalid_credentials` (kilitli/pasif de), (4) `auth.locked_out`/`auth.user_disabled` yalnız parola doğruyken. **Kilit yaklaşımı:** hesap kilit eşiği `Identity:MaxFailedAccessAttempts` = 10 (tüm IP'lerden), tek IP+hesap eşiği `LoginThrottleMaxFailures` = 5 / 15 dk (bellek içi `LoginThrottle`) — tek bir IP bilinen bir e-postayı tek başına kilitleyemez (eşiği aşmadan 429 alır); kilitliyken yanlış deneme kilidi **uzatmaz**; kilit süresi sabit 15 dk (kademeli artış yok). Dağıtık saldırıya (çok IP) karşı hesap kilidi + auth IP hız sınırı devrededir; kalan risk: çok IP'li saldırgan bir hesabı 15 dk'lık aralıklarla kilitleyebilir (kabul edildi, runbook §9.1).
- Refresh: bkz. §5 (atomik dönüşüm `IRefreshTokenRepository.TryRotateAsync`, 10 sn tolerans, 30 gün mutlak aile ömrü). Parola değişimi `RevokeAllOfUserAsync` ile tüm aileleri kapatır.
- Hash: PBKDF2-HMAC-SHA512, `Identity:PasswordHashIterations` = 210 000; eski (ASP.NET varsayılanı 100 000) hash'ler girişte doğrulanır ve yeniden hash'lenir (`SuccessRehashNeeded`).
- Access token 15 dk (`AccessTokenMinutes`); JWT `perm` claim'i **yoktur**: izinler her istekte `IPermissionService` (HybridCache) ile aktif üyelik + rolden çözülür, bu yüzden rol/üyelik değişikliği ≤ önbellek süresi (aşağıda) içinde etkili olur; değişiklikler ayrıca önbelleği hemen geçersiz kılar (`UpdateMember`, `UpdateRole`, davet ekleme/kabul).

### 16.4 İstek yüzeyi (M2, M6, M9, L4, L6–L8)
- **M2:** `GlobalLimiter` = zincir(kullanıcı `sub` başına, kiracı `tid` başına) sabit pencere (`RateLimiting:User` 600/dk, `Tenant` 3000/dk; anonim istekler sınırsız, auth uçları eski IP politikasında); `UseRateLimiter` artık `UseAuthentication`'dan **sonra**. Kestrel `MaxRequestBodySize` = `RequestLimits:MaxRequestBodyBytes` (varsayılan 1 MB, aşan 413).
- **M6:** `[AnyAuthenticatedUser("gerekçe")]` (Shared.Contracts) işareti; `Sense.Crm.Tests.Architecture.RequestAuthorizationTests` her `ICommand/IQuery` tipinin `[RequiresPermission]` ya da gerekçeli `[AnyAuthenticatedUser]` taşımasını, her controller eyleminin `[Authorize]` istemesini (anonim yalnız `AuthController` kimlik akışı: `GetConfig, SignUp, Login, Refresh, Logout`) zorlar; yeni modüller otomatik kapsanır.
- **M9:** Redis yoksa `Caching:PermissionExpirationMinutes` varsayılanı 2 dk (açıkça verilmemişse; Redis varsa 10). Tek örnek/önbellek notu: runbook §9.1.
- **L4:** JWT doğrulamada `ValidAlgorithms = [RS256]`; `Auth:KeyId` boşsa `kid` = imza anahtarının açık kısmının SHA-256 parmak izi (16 karakter; sabit `crm-dev` kalktı).
- **L6:** `PagedQueryBinder`: `page` > 1 000 000 ya da `int`'e sığmayan `page`/`pageSize` → 400 `validation`; `PagedQuery.Skip`/`SkipFor` `long` ile hesaplanıp `int.MaxValue`'ya kırpılır (taşma/500 yok); `pageSize` üst sınırı eskisi gibi kırpılır.
- **L7:** firma `website` yazmada mutlak `http(s)` URL veya boş (`OptionalHttpUrl`); eski çıplak-host kayıtlar okunurken hata vermez, düzenlenirken düzeltilmeli. **L8:** `FilterExpressionBuilder.BuildLike` kullanıcı girdisindeki `%`, `_`, `\` kaçışlar (`EscapeLike` + `ESCAPE '\'`).
- **L2:** `ActivityReadStore` `relatedName`'i yalnız çağıranın ilgili tür okuma izni (`crm.accounts|contacts|leads|deals.read`) varsa döner, aksi `***` (sistem bağlamı hepsini görür).

### 16.5 Bağımlılık ve imaj (L10)
`packages.lock.json` tüm projelerde (`RestorePackagesWithLockFile`); CI ve Dockerfile'lar `dotnet restore --locked-mode`. **Yeni proje eklenince** (yeni modül) `dotnet restore Sense.Crm.slnx` ile kilit dosyası üretilip commit edilmelidir (kilitli kipte eksik dosya restore'u kırar). `NU1901–NU1904` yeniden hatadır (taramada zafiyetli paket çıkmadı). İmajlar: `postgres:17.11-alpine`, `redis:7.4.11-alpine`, `aspnet:10.0.12` (SDK 10.0.401 ile aynı çalışma zamanı); `web/Dockerfile` (node/nginx) bu kartın kapsamı dışındaydı.

### 16.6 Testler
`Sense.Crm.Modules.Identity.Tests`: `AccountModelApiTests` (geçici parola/zorunlu değişim, parola değiştirme + tüm oturumların iptali, bekleyen davet/sızıntı/başkası kabul edemez/reddetme, politika), `LoginAndSessionHardeningApiTests` (genel hata sırası, IP+hesap/e-posta azaltma, eşzamanlı refresh, tolerans/hırsızlık, mutlak ömür, `kid`/`alg`, hash yükseltme, M2, Kestrel sınırı, sayfalama), `DelegationAndCacheApiTests` (M7, M9); `Sense.Crm.Modules.Workflows.Tests.TaskInputTrustApiTests` (H1, L3); `Sense.Crm.Modules.Activities.Tests.RelatedNameVisibilityApiTests` (L2); `Sense.Crm.Modules.Sales.Tests.AccountWebsiteApiTests` (L7); `Sense.Crm.Shared.Infrastructure.Tests.GridLikeEscapingTests` (L8); `Sense.Crm.Tests.Architecture.RequestAuthorizationTests` (M6).

## 22. Platform modülü (Milestone 7 — SaaS hazırlığı: planlar, limitler, kiracı yaşam döngüsü, platform konsolu, kullanım ölçümü, KVKK imhası)

Bağlayıcı plan + HTTP kontratı: [m7-saas-hazirlik.md](../plan/m7-saas-hazirlik.md) (karar K18: [kararlar.md](kararlar.md)). Ödeme sağlayıcısı, fatura ve tahsilat yoktur; PRD'deki "faturalama" maddesi yalnız ölçüm + dışa aktarma kancasıyla karşılanır.

### 22.1 Modül ve bağımlılıklar
- `Sense.Crm.Modules.Platform.{Domain, Application, Contracts, Infrastructure, Api}` (şema `platform`, `PlatformDbContext`, migration `InitialPlatform`). **Hiçbir modül Platform'a bağlanmaz**; Platform yalnız `Shared.*` + `Identity.Contracts` kullanır. `Sense.Crm.Modules.Platform.*`'ı yalnız `Api`, `Worker`, `Migrator` ve testler referans eder (`PlatformArchitectureTests`). Diğer modüller Platform'un davranışını `Shared.Contracts.Entitlements` arayüzleriyle görür.
- Platform tabloları **küreseldir** (`ITenantEntity` değil): `plans`, `tenant_accounts` (kiracı başına bir satır; `xmin` eşzamanlılık belirteci), `deletion_requests` (kısmi benzersiz indeks: kiracı başına tek etkin talep), `usage_snapshots` (PK `(tenant_id, day)`), `platform_audit_entries` (append-only). Kiracı yaşam döngüsü ve plan ataması yalnız `tenant_accounts`'tadır; `identity.tenants.is_active` M7'de kullanılmaz. Identity eki: `tenants.profile_completed_at` (ilk başarılı `PUT /organization`; mevcut kiracılar `null`).
- Test/host varsayılanı: `AddCrmCore` `UnlimitedEntitlements`/`AllowAllLimitGuard`/`AllowAllPlanCatalog`/`DenyPlatformAdminVerifier`'ı `TryAdd` eder (Platform yüklü değilse davranış değişmez); `PlatformModule`/`AddPlatformContractServices` gerçeklerini `Replace` eder, Identity `IPlatformAdminVerifier`'ı `Replace` eder. **Test hostları** (`CrmApiFactory`) `Platform:Signup:PlanCode` ve `Provisioning:DefaultPlanCode` = `internal` verir (limitsiz, tüm modüller açık, denemesiz) ve `platform.plans`'ı Migrator'ın aynı `PlanSynchronizer`'ıyla başlangıçta doldurur; Respawn `platform.plans`'ı sıfırlamaz. Yaşam döngüsü/limit testleri kendi planını `platform.plans`'a ekler.

### 22.2 Planlar (yapılandırma güdümlü) ve etkin haklar
- `Platform:Plans` → Migrator (`migrate`, `sync-plans`) `platform.plans`'a idempotent upsert eder (`PlanSynchronizer`); doğrulama `PlanCatalogValidator` (kod `^[a-z][a-z0-9_]{1,31}$`, limit ≥ 0, `trialDays` 1–365/null, yalnız kapı modülü bayrağı, kayıt/varsayılan plan katalogda; hata → Migrator çıkış 3). Yapılandırmadan kalkan plan `is_active = false` olur. `Platform` bölümü API ve Migrator `appsettings.json`'da **aynıdır** (test zorlar). Plan arayüzü yoktur; `GET /platform/plans` salt okunurdur.
- Kapı modülleri: `workflows | commerce | service | marketing`; `identity`, `sales`, `activities` çekirdektir. **Etkin değer = istisna (`overrides`) varsa o, yoksa plan** (`EffectiveEntitlements.Merge`; `maxUsers: null` = açıkça sınırsız, anahtar yok = plan).
- Limitler: `maxUsers` **sert** (etkin + bekleyen davetli; pasif saymaz), `maxRecords.<modül>` **yumuşak** (`Platform:Usage:CacheSeconds` = 300 sn önbellekli sayım; yaşayan kayıt toplamı; yumuşak silinenler sayılmaz). `null` sınırsız (**sıfır sayım sorgusu**), `0` yeni kayıt yok. Depolama limiti yoktur.

### 22.3 Yaşam döngüsü (saklanan + etkin durum)
Saklanan: `active | suspended | pending_deletion | deleted`. **Etkin durum** okuma anında saf fonksiyonla (`TenantLifecycle.Evaluate(status, suspensionMode, trialEndsAt, now)`; liste filtresi için aynı tablo `TenantStatusExpression` SQL ifadesi) türetilir; zamanlanmış iş yoktur: `trial | active | trial_expired | suspended | pending_deletion | deleted`, erişim `full | readOnly | none`. `now >= trial_ends_at` ise deneme bitmiştir; `trial_ends_at` = `trialEndsOn` gününden **sonraki** günün başlangıcı (kiracı saat diliminde; Europe/Istanbul'da `2026-12-31` → `2026-12-31T21:00:00Z`). `suspended` deneme bitişinden önceliklidir. Geçişler `TenantAccount` metotlarındadır; tanımsız geçiş `platform.invalid_transition` (409), sistem kiracısı `platform.system_tenant_protected` (422).

### 22.4 Zorlama: tek paylaşılan mekanizma
- `Shared.Contracts.Entitlements`: `ITenantEntitlements`, `ILimitGuard`, `IPlanCatalog`, `EntitlementSnapshot`, `TenantLifecycle`, öznitelikler `[ConsumesLimit]`, `[NoPlanLimit]`, `[TenantStatusExempt]`, `[EntitlementExempt]`; `Shared.Contracts.Security.[PlatformAdminOnly]` + `IPlatformAdminVerifier`; `Shared.Contracts.Usage.IUsageReporter`; `Shared.Contracts.Retention.ITenantDataEraser`; `IModuleUnitOfWork.AcquireAdvisoryLockAsync`.
- **`EntitlementBehaviour`** (`Shared.Infrastructure`; sıra Logging → Validation → Authorization → UnitOfWork → **Entitlement** → Caching): `[PlatformAdminOnly]`, kiracısız ve sistem bağlamı atlanır; erişim `none` (muaf değilse) ya da `readOnly` + komut → `403 tenant.suspended`; kapı modülü (isteğin assembly adından türer, komut başına bayrak yok) kapalıysa `403 plan.module_disabled` (okuma dahil); `[ConsumesLimit]` → `ILimitGuard` → `402 plan.limit_exceeded`. **Sert kullanıcı limiti** `LimitGuard`: sayımdan önce `pg_advisory_xact_lock(hashtextextended('limit:users:'||tenantId, 0))` (Identity'nin açık transaction'ında) → eşzamanlı üye eklemeler sıraya girer, limit asla aşılmaz; `UpdateMemberHandler` pasif üyeyi yeniden etkinleştirirken ikinci çağrı noktasıdır.
- **Ek noktalar** (aynı `ITenantEntitlements`): `InProcessEventBus` (erişim `Full` değilse veya kapı modülü kapalıysa işleyici **atlanır**: günlük + `crm.event_handlers.skipped` sayacı; `[EntitlementExempt]` işleyiciler — Sales `OrganizationCreatedHandler`, Service `OrganizationCreatedSlaHandler`, Platform'un `OrganizationCreated/Updated` işleyicileri — her zaman çalışır); Worker `ConductorTaskPollingService` (askı/`workflows` kapalı → görev `FAILED_WITH_TERMINAL_ERROR`, neden `tenant_suspended`/`module_disabled`); `SessionIssuer.ResolveDetailedAsync` (giriş: `none` kiracılar atlanır, hepsi `none` ise `403 tenant.suspended`; `switch-organization` `403 tenant.suspended`; refresh mevcut başarısızlık yolu).
- Önbellek: ham durum `ent:{tenantId}` (HybridCache, `Platform:Entitlements:CacheSeconds` = 30); Platform komutları aynı süreçte anında geçersiz kılar; **etkin durum her çağrıda saatten** hesaplanır. Satır yoksa `ITenantEntitlements` `Platform:Signup:PlanCode` planı + deneme ile `source = lazy` satırı açar (`INSERT … ON CONFLICT DO NOTHING`); `OrganizationCreated` işlenince düzeltilir. `GET /me` `subscription` alanını yazar; `/me`, `/subscription` ve kimlik akışı istekleri `[TenantStatusExempt]`'tir.
- **`[ConsumesLimit]`** (tam liste; mimari test birebir zorlar): `AddMemberCommand` (users) ve Records: Sales `CreateAccount/Contact/Lead/Deal`, Activities `CreateActivity`, Workflows `CreateRule`, Commerce `CreateProduct/Quote/Order` + `ConvertQuote`, Service `CreateCase`, Marketing `CreateCampaign`. Sales `CreatePipelineCommand` `[NoPlanLimit]`. Her `Create*Command` ya `[ConsumesLimit]` ya gerekçeli `[NoPlanLimit]` taşır (yeni modül otomatik kapsanır). `[TenantStatusExempt]` kümesi onaylı listeyle birebir eşit olmalıdır.

### 22.5 Platform güvenliği
`isPlatformAdmin` kiracı izni değildir (kataloğa `platform.*` anahtarı girmez). `/platform/**` denetleyicileri sınıf düzeyinde `[Authorize(Policy = "PlatformAdmin")]` (JWT bayrağı, hızlı ret; `JwtBearerEvents.OnForbidden` 403 `forbidden` ProblemDetails yazar) **ve** her istek `[PlatformAdminOnly]` (`AuthorizationBehaviour` → `IPlatformAdminVerifier`: hesap aktif + bayrak **veritabanında**; token'daki bayrağa güvenilmez). Her istek tam olarak birini taşır: `[RequiresPermission]` | `[AnyAuthenticatedUser]` | `[PlatformAdminOnly]`; `Platform.Application.Console` ad alanı `[PlatformAdminOnly]`, `…Tenant` `[RequiresPermission]`. `POST /platform/organizations` (Identity'de kalır) artık `[PlatformAdminOnly]` ve `planCode`/`trialEndsOn` alır (`IPlanCatalog` ile eşzamanlı doğrulanır). Platform, kiracı verisine yalnız sayaç (`IUsageReporter`, `BeginScope` ile kiracı kapsamında) düzeyinde erişir; `IgnoreQueryFilters` envanteri sabittir (§6). Platform denetimi (`platform_audit_entries`) iş verisiyle aynı transaction'da yazılır; kiracı denetim tablosuna yazılmaz; `details` kişisel veri içermez.

### 22.6 Kullanım ölçümü ve dışa aktarma
Her modülün Infrastructure'ı tam bir `IUsageReporter` kaydeder (`{modül}.{varlık}` + `{modül}.records`; Identity: `users_active`, `users_pending`, `profile_completed`; Commerce/Service/Workflows için `Add<Modül>ContractServices` yeni). Worker `UsageSnapshotService` (`Platform:Usage:SnapshotPollMinutes` = 30, `pg_try_advisory_lock` ile tek örnek, `UsageSnapshotJob`): bugün (UTC) için anlık görüntüsü olmayan `active|suspended` her hesap için idempotent upsert; kiracı hatası diğerlerini engellemez; saklama temizliği aynı işte (`Usage:RetentionDays` 400, `Audit:RetentionDays` 1825). Eski günler geriye dönük üretilemez. `GET /platform/usage/export` UTF-8 BOM'lu CSV'yi **akış halinde** yazar (`day,tenantId,slug,name,planCode,status,usersActive,usersPending` + `metrics` anahtarlarının alfabetik birleşimi; `status` dışa aktarma anındaki etkin durum; formül enjeksiyonu önekli) ve `usage.exported` denetimini yazar.

### 22.7 KVKK imhası
`POST …/deletion-request` (kiracı anında `pending_deletion`, bekleme 7–90 gün, varsayılan 30) → `TenantErasureService` (`Platform:Deletion:PollMinutes` = 10) → `TenantErasureJob`: adımlar `ITenantDataEraser` `Order` sırasıyla, idempotent, `erased_steps` ile yeniden başlatılır; hata → `failed` + üstel bekleme + `MaxAttempts` sonrası `deletion.failed`. Adımlar: `workflows-conductor` (90; yürütmeleri Conductor'dan `IWorkflowEngine.RemoveAsync` ile siler), **`identity-accounts` (50)** (yalnız bu kiracıya ait hesapları + oturumları ve tüm üyelikleri tek transaction'da siler; ortak/platform yöneticisi hesapları kalır — üyelikler genel adımdan önce okunmak zorunda olduğundan plandaki 800 yerine 50), `module:<şema>` (100; **`AddModuleDbContext` ile otomatik** kayıtlı `TenantDataEraser<TContext>`: EF metadata'sından tüm `ITenantEntity` tabloları + outbox, FK'ye göre çocuk önce, `ctid` parçalarıyla), `identity-tenant` (800; kiracı satırı + kalan refresh token + izin önbelleği), `audit` (900; `audit.audit_log_entries`), mezar taşı (`platform-tombstone`: hesap `deleted`, ad `[deleted]`, slug `deleted-xxxxxxxx`, `usage_snapshots` silinir, denetim hedef adları redakte, talep `completed` + rapor, `TenantErased` olayı, `deletion.completed`). Bilerek kalanlar: `platform_audit_entries`, `deletion_requests`, mezar taşı, ortak hesaplar, yedekler. Sistem kiracısı asla imha edilmez. Migrator `erase-deleted-tenants` tüm mezar taşları için imhayı yeniden koşar (geri yükleme sonrası). `inbox_messages` kiracı anahtarı taşımaz ve kullanılmaz (kapsam dışı).

### 22.8 Uçlar (özet; ayrıntı plan belgesinde)
Konsol (yalnız platform yöneticisi): `GET /platform/organizations` (`q`, `status` = **etkin** durum, `planCode`, `source`, `sort` name/createdAt/users/plan/status), `GET …/{tenantId}`, `PUT …/{tenantId}/subscription` (`{ planCode*, trialEndsOn?, overrides? }` → `{ overLimit }`), `POST …/suspend` (`{ reason*, mode?: readOnly|blocked }`), `…/reactivate`, `…/deletion-request` (`{ reason*, retentionDays? }`), `…/deletion-request/cancel`, `GET …/usage`, `POST …/usage/refresh`, `GET /platform/usage/export`, `GET /platform/plans`, `GET /platform/audit`. Kiracı: `GET /subscription`, `GET /onboarding`, `POST /onboarding/dismiss` (`org.settings.manage`), `GET /me` (`subscription`).

### 22.9 Yapılandırma, testler, sapmalar
- Yapılandırma: `Platform:{Signup, Provisioning, Plans, Entitlements, Usage, Audit, Deletion}` (ortam değişkeni `Platform__…`; ayrıntı: runbook §14).
- Testler: `Sense.Crm.Modules.Platform.Tests` (yaşam döngüsü/etkin hak birleştirme/CSV saf testleri; `EntitlementBehaviour` zorlama matrisi; limit sınırları ve eşzamanlılık; askı ve deneme bitişi (sahte saat); konsol, denetim, ölçüm, onboarding; imha; kiracı izolasyonu ve **tüm `/platform/**` uçlarının kiracı yöneticisine 403** olduğu), `Sense.Crm.Tests.Architecture` (`RequestAuthorizationTests`: üç öznitelik + ad alanı + `/platform` politikası + muaf/`ConsumesLimit` listeleri; `PlatformArchitectureTests`: bağımlılık, her modülün `IUsageReporter`'ı, yapılandırma eşitliği), `Sense.Crm.Tests.TenantIsolation` (`TenantFilterBypassInventoryTests`; `GlobalEntities` Platform tipleri). `TestClock` `Tests.Shared`'a taşındı.
- Plan kontratından **bilinçli sapmalar:** (1) kimliksiz akış komutları (`LoginCommand`, `RefreshTokenCommand`, `LogoutCommand`, `SignUpCommand`) `[TenantStatusExempt]`'e eklendi (JWT kimlik doğrulaması `[AllowAnonymous]` uçlarda da çalıştığından Bearer başlığı kiracı bağlamı kurabilir; askıdaki kiracının kullanıcısı çıkış yapabilmeli); (2) Identity hesap imhası sıra 50'de (bkz. §22.7) + kiracı satırı ayrı adımda (800); (3) `inbox_messages` imha kapsamı dışı; (4) değişmeyen (ör. yalnız istisna düzenlenirken yeniden gönderilen) deneme tarihi geçmişte olsa da kabul edilir, **yeni** geçmiş tarih `400 errors.trialEndsOn`; (5) `ModuleCatalog`'daki her **iş** modülünün tam bir `IUsageReporter`'ı vardır (Platform hariç); (6) platform tablolarındaki `created_at/modified_date` kolonları `Entity<T>` taban sınıfından gelir (`updated_at` yerine).
- Açık işler: kendi kendine kayıtta CAPTCHA/e-posta doğrulaması yok (dış SaaS öncesi ayrıca ele alınmalı); çok kopyada varlık önbelleği ≤ 30 sn bayat (runbook §9.1); imha yedekleri temizlemez; ~~depolama limiti yok~~ — **M8C ile kapandı** (`LimitKeys.Storage` + `files.storage_bytes` metriği + `maxStorageMb`, bkz. §23).

## 23. Files modülü (Milestone 8C — dosya ekleri: nesne depolama, kayıt ekleri, kota, yaşam döngüsü)

Bağlayıcı plan + HTTP kontratı: [m8c-dosya-ekleri.md](../plan/m8c-dosya-ekleri.md) (karar K19: [kararlar.md](kararlar.md); işletim: runbook §15). Sürümleme, paylaşım bağlantısı, OCR, ofis önizlemesi ve virüs tarama **uygulaması** kapsam dışıdır.

### 23.1 Modül ve bağımlılıklar
- `Sense.Crm.Modules.Files.{Domain, Application, Contracts, Infrastructure, Api}` (şema `files`, `FilesDbContext`, migration `InitialFiles`). Files yalnız `Shared.*` ve `Identity.Contracts` kullanır; **hiçbir iş modülü Files'a, Files hiçbir iş modülüne bağlanmaz** (`FilesArchitectureTests`). Kayıt bağı `Shared.Contracts.Files.IAttachmentTarget` (tür anahtarı + okuma/yazma izni + toplu varlık sorgusu): 9 tür, her biri **sahip modülün** `Infrastructure/AttachmentTargets.cs`'inde (Sales 4, Commerce 2, Service 1, Activities 1, Marketing 1) ve o modülün `Add<Modül>ContractServices()`'ine kayıtlı; yeni tür = yeni bir sınıf.
- **Yeni izin yoktur** (D3): erişim kararı **kaydın kendi izniyle** çalışma anında verilir (`IAttachmentAccess`, tek koruma sınıfı; sıra: tür → izin → kapı modülü → kayıt varlığı; dosya kimliğiyle okumada izinsiz = `404`). İstekler gerekçeli `[AnyAuthenticatedUser]` taşır; her handler ilk iş `IAttachmentAccess` çağırır (mimari test zorlar; tek istisna kiracı-bağımsız `GET /files/limits`, `GET /files/usage` `org.settings.manage`).
- Kompozisyon: `IAttachmentAccess` yalnız API'de (`FilesModule`) kayıtlıdır; Worker/Migrator `AddFilesContractServices(configuration)` çağırır (depolama, işler, `IUsageReporter`, `FilesObjectEraser`) ve Files Application assembly'sini **taramaz**.

### 23.2 Depolama
- `IFileStorage` (akış tabanlı, `ObjectKey` alır) + adaptörler: **`S3FileStorage`** (`AWSSDK.S3` 4.0.x; `RequestChecksumCalculation/ResponseChecksumValidation = WHEN_REQUIRED`, `ForcePathStyle`; üretim), `FileSystemFileStorage` (Development), `InMemoryFileStorage` (Testing/birim). Üretimde `Provider != s3` ya da `Encryption=none` (beyansız) **başlatmaz**. Anahtar: `{tenantId:D}/{yyyy}/{fileId:N}` (tek oluşturma yolu `ObjectKey.For`, katı `Parse`; kullanıcıdan gelen hiçbir parça anahtara girmez); nesne deposunda `Content-Type` daima `application/octet-stream`, ad/`Content-Disposition` **saklanmaz**. Her adaptör metodu kiracı bağlamıyla anahtar kiracısını karşılaştırır (uyuşmazlık = `InvalidOperationException` + güvenlik günlüğü); DB'den okunan `storage_key` her kullanımdan önce yeniden ayrıştırılır (kurcalanmış satır → `404`, bayt sızmaz).
- `CheckAsync`: erişim + kova + varsayılan SSE-S3 (`Encryption=required`); şifreleme doğrulanamıyorsa **yükleme reddedilir** (`503 file.storage_unavailable`, sessizce şifresiz yazılmaz). `/health` `storage` denetimi (`Degraded`, **ready etiketi yok**).

### 23.3 Yükleme akışı ve güvenlik
- İki faz: **faz 0** `AuthorizeUploadCommand` (izin/plan/varlık/`tenant.suspended`; hiçbir bayt okunmadan) → `Content-Length > MaxRequestMb` ise 413; faz 1 `IUploadStager` (`MultipartReader` elle, parça parça: `FileNamePolicy` → uzantı allow-list → geçici dosya + bayt sayımı + SHA-256 tek geçişte, `MaxFileMb` aşılınca hemen kes → 0 bayt → `FileTypeCatalog` içerik imzası + beyan → `IFileScanner`); faz 2 `AttachFileCommand` (UoW işlemi açık: erişim yeniden → **kota** → `PutAsync` → satır + `FileAttached` outbox → commit; PUT sonrası kaydetme hatasında nesne `DeleteAsync` ile telafi edilir, başarısızsa uzlaştırma yetimi siler). Her `file` parçası bağımsız: hepsi başarılı `201`, karışık `200 { items, failed }`.
- `FileNamePolicy` (NFC, yol/kontrol/çift yönlü/`<>:"|?*` temizleme, `..`, Windows aygıt adları, 200 birim, çift uzantı reddi), `FileTypeCatalog` (kodda; yapılandırma yalnız **daraltır**; imzalar: pdf, jpeg, png, gif, webp, OOXML (merkezi dizin, makro/yürütülebilir reddi, ≤ 20 000 giriş), OpenDocument, OLE2, txt/csv (UTF-8, HTML/SVG başlangıcı yok)). Sunum: kanonik tür, `nosniff`, `Content-Disposition` (ASCII yedek + `filename*`), `private, no-store`, `ETag` (sha256) + `If-None-Match` 304, tek `Range` 206/416, `disposition=inline` yalnız png/jpeg/gif/webp/pdf (sandbox CSP + `X-Frame-Options: SAMEORIGIN`). **Dosya adı, içerik, imza ve `storage_key` günlüğe yazılmaz** (testli: günlük havuzunda ad geçmez).

### 23.4 Veri modeli ve denetim
- `files.attachments` (`FileAttachment`, `IAuditLogged`; `sha256`/`storage_key` denetimde maskelenir) ve `files.file_access_log` (append-only; kim/ne/ne zaman, **IP ve ad yok**; yalnız ilk parça sayılır). `state`: `ready | quarantined | missing | deleted` (küçük harf); silme **yumuşaktır** (kotadan anında düşer; nesne `SoftDeleteRetentionDays` sonra Worker'da silinir); `global is_deleted` filtresi kullanılmaz. İndeksler kiracı önceliklidir (kısmi indeksler: kayıt listesi, kota taraması `INCLUDE (size_bytes)`, temizlik, kayıt-yok). Denetim `EntityType` = `FileAttachment` (K14: CLR tip adı).
- Olaylar (`Files.Contracts`): `FileAttached`, `FileDeleted` (ad taşımaz; tüketici yok; süpürme `ActorUserId = null`).

### 23.5 Kota (M7 entegrasyonu)
- `LimitKeys.Storage`, `EntitlementSnapshot.MaxStorageMb` (isteğe bağlı son parametre), `PlanLimits/TenantOverrides/EffectiveLimits/…Dto` genişledi; `LimitGuard` `storage` için **Files UoW**'sinde `pg_advisory_xact_lock(hashtextextended('limit:storage:'||tenantId,0))` alır ve `files.storage_bytes`'ı kesin/önbelleksiz okur; `null` sınırda sayım/kilit **yoktur**; `used + delta > max` → handler `plan.limit_exceeded`'ı `file.quota_exceeded`'a çevirir. `PUT …/subscription` `overLimit`'e `{ limit: "storage", module: "files", max, used }` (bayt) ekler; `GET /subscription` `limits.maxStorageMb?`, `usage.storageBytes/fileCount`. `FilesUsageReporter` (`files.files`, `files.storage_bytes`; `files.records` yok) günlük anlık görüntüye ve finans CSV'sine girer.

### 23.6 Yaşam döngüsü, imha, uçlar
- Worker: `FilesPurgeService` (60 dk) ve `FilesReconciliationService` (24 sa) (`PlatformJobService` iskeleti; `pg_try_advisory_lock`); `migrator files-reconcile [--dry-run] [--tenant <id>]` aynı kod. Uzlaştırma: nesnesiz satır → `missing`, boyut uyuşmazlığı, grace sonrası yetim silme (**güvenlik supabı**: `MaxOrphanDeletePerRun`/`MaxOrphanDeleteFraction`), yabancı anahtar ve bilinmeyen kiracı öneki **asla silinmez**, kayıt-yok süpürmesi (`IAttachmentTarget.GetExistingAsync`).
- KVKK: `FilesObjectEraser` (`files-objects`, sıra **95**) önekle siler + `ListAsync` ile 0 nesneyi **doğrular** (aksi adım `failed`); satırlar/erişim günlüğü genel `TenantDataEraser<FilesDbContext>` (100) ile gider; `migrator erase-deleted-tenants` geri yüklenen nesneleri yeniden siler.
- Uçlar (`/api/v1/files`): `GET` (liste), `POST` (multipart yükleme), `GET usage`, `GET limits`, `GET/PATCH/DELETE {id}`, `GET {id}/content`. Hız sınırı: yükleme parça başına kullanıcı/dk 30 (`files-upload`), indirme 120 (`files-download`), kiracı başına eşzamanlı yükleme 4 / genel 16 → `429 general.rate_limit_exceeded`.
- Ham SQL envanteri (`TenantFilterBypassInventoryTests`): Files **+1** dosya (`FilesJobs.cs` — temizlik işinin tek parametreli kiracı listesi `SqlQuery`); yeni `IgnoreQueryFilters` **yok**. Plan +3 öngörmüştü; uzlaştırma kiracıları `ITenantDirectory`'den, imha genel `TenantDataEraser`'dan aldığı için yalnız +1 gerekti.
- `ErrorType`'a 413/415/410/503 eklendi (`PayloadTooLarge`, `UnsupportedMediaType`, `Gone`, `Unavailable`).

### 23.6.1 Plan kontratından bilinçli sapmalar
(1) Denetim `entityType` değeri `"file"` değil **`FileAttachment`** (K14 CLR tip adı; `AuditLogInterceptor` bunu belirler); (2) `files.attachments`'a **`renamed_at`** kolonu eklendi (`FileDto.updatedAt` "yalnız yeniden adlandırıldıysa" kuralını `modified_date` bulanıklığı olmadan sağlamak için); (3) `ProcessPart` hız sınırı ve fazla parça (`file.too_many_files`) **parça başına** hata olarak `failed[]`'de döner (kısmi başarıda 200; hiçbiri başarılı değilse ilk hata); (4) ham SQL envanteri +1 (yukarıda); (5) `Files:Scanner:Provider` yalnız `none` kabul edilir (ClamAV bağdaştırıcısı yok → başka değer açılışta reddedilir); (6) web nginx/CSP değişiklikleri (`web/`) bu karta dâhil değildi: runbook §15.2'de tam yönerge var.

### 23.7 Testler
`Sense.Crm.Modules.Files.Tests` (birim: `FileNamePolicy`, `ObjectKey`, `FileTypeCatalog`, `HttpRange`, yapılandırma doğrulaması; **depolama sözleşme testleri InMemory + FileSystem + gerçek MinIO Testcontainer'ı**, şifreleme/sağlık; HTTP: yükleme/indirme/aralık/önizleme/başlıklar, doğrulama ve güvenlik, İzin matrisi 9 tür × 7 rol × 7 uç, kapı modülü, askı/salt okunur, kota (sınırlar, eşzamanlılık: 8 yükleme → tam 2 başarı), kiracı izolasyonu (kurcalanmış satır dâhil), yaşam döngüsü (temizlik, uzlaştırma, güvenlik supabı, süpürme), KVKK imhası); `Sense.Crm.Tests.Architecture.FilesArchitectureTests`. MinIO imajı `CRM_TEST_MINIO_IMAGE` ile ezilebilir (varsayılan sabit etiket). Canlı duman testi: `build/smoke-files.ps1`.
